# 観察画面の環境アセット(M22-02 デフォルメ試作)

鐘樹・集落・地面の植生を、Switch 世代の 3D ポケモン程度の密度でモデルにしたものです。
形は `assets/textures/board/sheets/{belltree,settlement,flora}.png` から取り、画風は動物の基準画 `assets/textures/board/creatures/*.png` に寄せました。
予算は `docs/design/2026-09-23-observation-view-design.md` §8 に従います。

## 作り方と確認

| 何を | どこで |
|---|---|
| 共通の部品(材質・形の部品・頂点色・柔らかい法線・書き出し) | `tools/blender/observe_kit.py` |
| 鐘樹 | `tools/blender/observe_belltree.py` → `assets/models/observe/belltree.glb` |
| 集落の部品 | `tools/blender/observe_settlement.py` → `assets/models/observe/settlement.glb` |
| 植生 | `tools/blender/observe_flora.py` → `assets/models/observe/flora.glb` |
| 読み直し・並べ図・集落の一角 | `tools/blender/observe_render.py`(`verify` / `lineup` / `corner`) |

```sh
blender -b --factory-startup --python tools/blender/observe_belltree.py
blender -b --factory-startup --python tools/blender/observe_settlement.py
blender -b --factory-startup --python tools/blender/observe_flora.py
blender -b --factory-startup --python tools/blender/observe_render.py -- verify assets/models/observe/{belltree,settlement,flora}.glb
blender -b --factory-startup --python tools/blender/observe_render.py -- lineup assets/models/observe/belltree.glb docs/design/qa/observe/belltree.png --ref assets/models/deer.glb
blender -b --factory-startup --python tools/blender/observe_render.py -- corner docs/design/qa/observe/corner.png
```

GLB の約束:

- 単位はメートル、Y 上です。各ノードは平行移動・回転を持たず、原点が地面の中心です。根や土の盛りは地面より最大 9 cm 沈めてあります。
- 1 ノードに 1 メッシュで、材質ごとに primitive が分かれます。属性は `POSITION`・`NORMAL`・`COLOR_0` だけです(UV はありません)。
- 材質は平らな基本色(`baseColorFactor`)です。`COLOR_0` は陰りと石ごとのばらつきを表す 0〜1 の乗数で、Three.js では `vertexColors: true` にすると glTF の規約どおり `baseColorFactor × COLOR_0` になります。
- 発光は `emissiveFactor` と `KHR_materials_emissive_strength` で持ちます。
- 草の葉(`flora_grass`・`flora_moongrass`)だけ `doubleSided` です。
- 葉の塊・草・苔には「柔らかい法線」を入れました。塊の法線と樹冠の中心から外への向きを混ぜたカスタム法線で、トゥーンのランプで一つの量感として陰ります。

## 三角形数(書き出した .glb を Blender で読み直した実測)

| ノード | 三角形 | 予算 | 大きさ(幅 × 奥 × 高さ m) | 材質 |
|---|---:|---:|---|---|
| `belltree_seedling` | 150 | — | 0.40 × 0.37 × 0.38 | soil, moss, bark, leaf, glow |
| `belltree_sapling` | 468 | — | 1.47 × 1.45 × 2.95 | soil, moss, bark, leaf |
| `belltree_mature` | 3,646(木の磨き上げで 3,917 から) | ≤ 4,000 | 11.64 × 11.55 × 11.17(木の磨き上げ: 葉のカードの角まで。前は 9.85 × 9.91 × 10.22) | trunk, bark, leaf, foliage, bell, bell_rim(鐘 30 個、M22-06 で裾を開いた、M22-07 で樹冠を房 8 つに分けた、木の磨き上げで葉のカード 253 枚) |
| `belltree_mature_lod1` | 1,080(木の磨き上げで 1,112 から) | ≤ 1,200 | 12.61 × 12.39 × 11.36(木の磨き上げ: 葉のカードの角まで。前は 10.24 × 10.09 × 10.28) | trunk, bark, leaf, foliage, bell, bell_rim(鐘 30 個、位置は成木と同じ、M22-07 で房 8 つ、木の磨き上げで葉のカード 105 枚) |
| `belltree_stump` | 300 | — | 2.06 × 2.21 × 0.87 | soil, moss, bark, cut, cut_ring |
| `belltree_logs` | 432 | — | 2.14 × 1.28 × 1.08 | cut, cut_ring, bark |
| `hut` | 1,934 | 1,200〜2,000 | 4.73 × 4.12 × 3.85 | straw, stone, moss, wood, bark, vine |
| `lantern_post` | 512 | ≤ 600 | 1.36 × 0.86 × 2.35 | stone, moss, rope, frame, lantern(M22-06 で灯籠に X の格子) |
| `slipway` | 1,982(1,114 から) | ≤ 2,500(≤ 1,500 から) | 8.24 × 27.78 × 2.74(5.11 × 16.54 × 1.81 から) | stone, moss, wood(M22-06 試作 2 の判断で大きな舟に合わせて 27 × 7.6 m に広げた。`keyitems.md`) |
| `stone_wall` | 440 | ≤ 600 | 4.04 × 0.67 × 0.96 | stone, moss |
| `megalith` | 596 | ≤ 800 | 1.98 × 1.79 × 3.78 | glyph, stone, moss |
| `grass_tuft` | 40 | ≤ 40 | 0.51 × 0.57 × 0.35 | grass |
| `moongrass_tuft` | 40 | ≤ 40 | 0.43 × 0.55 × 0.63 | moongrass |
| `moss_clump` | 116 | ≤ 120 | 0.78 × 0.65 × 0.25 | moss, spore |
| `rock` | 160 | ≤ 200 | 1.26 × 0.85 × 0.52 | rock, moss |

大きさは根・床石・割れ石を含みます。石垣と巨石の差し渡しは根元の石のぶん大きくなっています。

## 材質

| 材質 | 基本色 | 発光 | 使う所 |
|---|---|---|---|
| `belltree_bark` | #E6DFD1 | — | 白い幹・枝・根・鐘の吊り紐 |
| `belltree_leaf` | #86A044 | — | 葉の塊・若木と芽の葉 |
| `belltree_bell` / `belltree_bell_rim` | #7E5230 / #FFD58F | #FFC46B × 0.2 / × 3.0 | 青銅の鐘の胴と、裾の帯と口(M22-06 で変更、`keyitems.md`) |
| `belltree_glow` | #FFE7A8 | #FFD98A × 3.0 | 芽の先の光 |
| `belltree_soil` / `belltree_moss` | #7B6043 / #8AA743 | — | 根元の土の盛り(上を向いた面が苔) |
| `belltree_cut` / `belltree_cut_ring` | #EFD6A8 / #D2AC7B | — | 株と丸太の断面(年輪の帯) |
| `settlement_stone` / `settlement_moss` | #A3A194 / #8FA548 | — | 石(石ごとに明るさをばらす)と上面の苔 |
| `settlement_bark` / `settlement_wood` / `settlement_vine` / `settlement_straw` | #8E6240 / #A07A52 / #6E8235 / #C19C5E | — | 屋根の樹皮、梁・竿・盤木、蔓、敷き藁 |
| `settlement_lantern` / `settlement_frame` / `settlement_rope` | #FFC77A / #6A4A2F / #B79E6E | #FFC46B × 2.5(灯籠) | 灯籠の胴、枠、吊り紐 |
| `settlement_glyph` | #8FF5E6 | #8FF5E6 × 1.5 | 巨石の割れ目と六角の紋(ムーのシアン) |
| `flora_grass` / `flora_moongrass` | #86A84C / #BCD0B4 | — | 草・月草(両面) |
| `flora_moss` / `flora_spore` | #88A83F / #E9F5A6 | #DDF28A × 1.5(胞子) | 胞子苔と、苔の乗った石 |
| `flora_rock` | #9C9A8D | — | 石 |

## 画像

EEVEE で、Three.js 側の陰影の予定(設計 §8)を近似しました。
地の色は `baseColorFactor × COLOR_0` で、白い拡散の明るさを 3 段のランプ(寒色の影・中間・暖色の光、境界はなめらか)に通し、縁の光と発光を足しています。
bloom はかけていません。並べ図の右端の月鹿(`assets/models/deer.glb`、高さ 1.6 m)は大きさの物差しです。

| 画像 | 中身 |
|---|---|
| `belltree.png` | 芽・若木・成木・成木 lod1・株・丸太・月鹿 |
| `belltree_small.png` | 芽・若木・株・丸太・月鹿の寄り |
| `settlement.png` | 小屋・灯り柱・船台・石垣・巨石・月鹿(船台は 16 m の旧版。広げた船台は `ship.png`・`shipyard.png`) |
| `settlement_small.png` | 船台を除いた寄り |
| `flora.png` | 草・月草・胞子苔・石 |
| `corner.png` | 集落の一角(小屋、灯り柱 2、船台、石垣、巨石、成木 3、株、丸太、若木、芽、散らした草・月草・苔・石、月鹿)を低い斜めから(船台は 16 m の旧版のまま描き直していない) |

## 基準画との照合(自己評価)

形(sheets):

- 鐘樹: 幅の広い丸い樹冠、白く太い幹と根張り、樹冠の下に分かれる枝、垂れた鐘という骨格は合っています。
  基準画の樹冠は細かい葉の房が縁で毛羽立っていますが、こちらは滑らかな塊の集まりです。鐘は寄ると鐘に見え、引くと橙の円錐に見えます。
- 若木: 細い白い幹に大きな卵形の葉が螺旋に付き、縦長の輪郭で基準画に近いです。株(裂けた樹皮の縁と年輪)と丸太 3 本も合っています。丸太の樹皮には筋がありません。
- 小屋: 四隅の巨石の柱、低い空積みの壁、樹皮の板の屋根、屋根にかかる蔓、棟で交差する竿という基準画の要素は揃っています。
  基準画の板は不揃いで毛羽立っていますが、こちらは揃った板です。
- 灯り柱: 苔の乗った石の柱、石の腕木、吊り灯籠です。基準画の灯籠は木の格子(X の組み)ですが、こちらは六角の胴に縦の枠だけです。
- 船台: 斜路、両脇の縁石、上端の柱石は基準画どおりです。竜骨を受ける木の盤木と滑り材は、舟を組む場所として足しました(基準画にはありません)。
- 石垣: 2 段の丸めた石と、端の太い石です。基準画は L 字に曲がり、端の石に紋が刻まれていますが、こちらはまっすぐな 4 m の一片です(曲がりは置き方で作る想定)。
- 巨石: 基準画にはありません。一本の立石を 3 つに割って積み直し、割れ目を淡いシアンに光らせ(月鹿の装甲の継ぎ目と同じ意匠)、上の石に六角の紋(土兎の紋)を置きました。
  最初に入れた線の紋は文字や人の形に見えたので、継ぎ目と六角だけに減らしました。
- 植生: 草・月草・苔・苔の乗った石は基準画の区別(太い草、細く淡い月草、胞子の粒の光る苔)が付きます。月草の穂はありません。

画風(creatures):

- 合っているところ: 丸めた角ばり(面取りした石、滑らかな葉の塊)、柔らかい量感(柔らかい法線と頂点色の陰り)、控えめなシアンの発光(巨石だけ)、色味(黄褐・青緑・苔緑・白い幹・暖かい灯り)。
  月鹿と並べても浮かない色と密度です(`corner.png`)。
- 届いていないところ: 基準画の絵画らしさ(面の中の色の揺らぎ、筆の跡、縁の毛羽立ち)は、平らな基本色と頂点色だけでは出ません。
  Three.js 側の色調補正と紙の粒、葉の塊の輪郭の揺らし(頂点シェーダ)、必要なら小さなテクスチャで補う前提です。

## 既知の課題

- UV を書き出していません(`export_texcoords=False`)。テクスチャ(石と木のアトラス、葉のカード)を足すときは、UV を作るように部品を直す必要があります。
- 材質はスペキュラ 0 のため `KHR_materials_specular` が付き、GLTFLoader は `MeshPhysicalMaterial` を作ります。動物の .glb(`tools/blender/lowpoly_kit.py`)と同じ扱いです。
  観察画面では読み込み後にトゥーンの材質へ差し替える想定です(設計 §8)。
- 苔は面ごとの材質の切り替えなので、面取りした石の上面が丸ごと緑になり、境界がくっきりします。
- 鐘は引きの距離で円錐に見えます。灯りの読みは bloom(Three.js 側)に頼っています。
- 風の揺れの重みは頂点属性で持っていません。草・葉は高さ(`position.y`)に比例させる想定です。
- LOD は成木だけです。小屋・船台などの遠景用 LOD はありません(予算の範囲内なので未作成)。
- 描画は EEVEE の近似です。Three.js のトゥーン・縁の光・色調補正での比較画は、M22-02 の本体の組み立てで撮り直します。
- 集落の衝立(`sheets/settlement.png` の編んだ衝立)は、今回の対象外として作っていません。

## 追記(M22-06 / M22-03)

舟の 6 段・丸太の山・衝立・L 字の石垣・森の木・羊歯・小花・穂の出た月草を足し、鐘樹の鐘と灯籠を直しました。三角形数・画像・自己評価は `keyitems.md` にあります。
上の表の鐘樹の成木・lod1・灯り柱の数は直した後の実測に更新しました。上の「既知の課題」のうち、鐘が引きで円錐に見える点と、衝立を作っていない点は `keyitems.md` で対応済みです。

## 追記(M22-07)

鐘樹の成木と lod1 の樹冠を、光の筋のために離れた房 8 つに組み直しました。上の表の成木・lod1 の数は組み直した後の実測に更新しました。房の配置・日が抜ける割合・確かめの図(`canopy-gaps.png`)は `keyitems.md` の「追記(M22-07 光の筋のための樹冠の隙間)」にあります。

## 追記(試作 3 の判断: 樹冠の房を詰める)

試作 3 への判断(引くと房が刈り込んだ木に見える)を受けて、成木と lod1 の房を大きくして隙間を詰め、光の筋が出る程度の狭い隙間だけ残しました。上の表の成木・lod1 の大きさは直した後の実測に更新しました(三角形数は同じ)。
日が抜ける割合は成木 0.10〜0.15、lod1 0.08〜0.16(前は 0.20〜0.34)です。房の値と確かめの図は `keyitems.md` の「追記(試作 3 の判断: 樹冠の房を詰める・船首の渦巻き)」にあります。`belltree.png`・`corner.png` は直した樹冠で描き直しました。

## 追記(草の磨き上げ)

審査台への判断(「草や木をひっとうにまだ磨き上げる余地はある」)のうち、草と地面を直しました。木は別の作業です。
比較画は左が前、右が後です: `grass-before-after-grove.png`(林)・`grass-before-after-settlement.png`(集落)・`grass-before-after-rabbit.png`(兎に寄った低い目)・`grass-before-after-wolf.png`(狼)。
撮り方は `observe.html?time=0.12&freeze=1&ship=60&shot=<寄せ先>`、兎は `__observeLook('rabbit', 6, 0.9, 0.5)` です(兎は動くので、前と後で立ち位置は揃いません)。

直したところ:

- 房の形: `flora.glb` の `grass_tuft`(8 枚の葉が星形に開く)をやめ、`src/observe/render/grass.ts` の `carpetTuft` にしました。
  12 枚の細い葉を根元に寄せて立て、内側の 4 枚は高くまっすぐ、外側は低く外へ撓みます。1 房 36 三角形(前は 40)で、その分を 25,000 → 30,000 房に回しました。
- 房の色: 根元は地面の色の陰(地面の色 × 0.55)、先は地面の色から若草へ寄せた色です。大きな斑で乾いた所は黄、茂った所は濃い緑、苔の所は苔の緑に寄せ、1 房ずつ明るさを ±8% 揺らします。
- 光: 葉の法線を上へ 8 割寄せ、地面と同じ陰りにしました。前は葉ごとに明暗が跳ね、星形の判に見える原因でした。影も受けます(前は木陰でも明るいままでした)。
- 遠くの房: 25 m から 80 m にかけて、房の色を根元の地面の色へ 8 割まで溶かします。距離での間引き(`grassKeep`)はそのままです。
- 地面: 色の式を `groundBase`・`groundJitter` にまとめ、草の根元の色と共有しました。40 m ほどの斑(茂った/乾いた暖かい)、15 m ほどの土ののぞく斑、水際 1.2〜3 m の湿り、森の木の陰を足しました。
  画素ごとに 0.5〜3 m の筆のむら(片方向に伸ばした値ノイズ、明るさ ±6% と色の振れ)と、25 m までの草地に 15 cm ほどの細かな斑を足しました。
- 置き方: 草の房の置き方と丈を地面と同じ斑(`groundPatch`)から決めます。土ののぞく所は疎らに、5 m ほどの寄り集まる斑は密に高く、乾いた斑は低くなります。
- 踏み固めた所: 集落の広場・3 軒の戸口への道・船台への道を踏み固めた土の色にし、そこの房を減らし、縁に残る房は短く乾いた色にしました(`wearTerrain`・`grass.trample`)。
- 風: 風向きを世界で揃え(前は房ごとの回転で向きがばらばらでした)、大きな突風の帯で強弱を付け、葉の先ほど大きく撓ませます。

兎: 房の丈は横の広がりと切り離し、最も高い葉の平均を約 0.47 m にしました(単体テストの草地 0.4 での値。前の `grass_tuft` は同じ式からの見積もりで約 0.53 m)。外側の 8 枚は内側の 5〜8 割の高さなので、座高 0.35 m の兎は草の上に頭と背が出ます(`grass-before-after-rabbit.png`)。
単体テスト `tests/unit/observe.grass.test.ts` で、丈の平均 0.5 m 未満・最大 0.85 m 未満を確かめています。

予算(`__observeStats`、1280×720、`time=0.12&freeze=1&ship=60`):

| 寄せ先 | draw call 前 → 後 | 三角形 前 → 後 | 見えている房 前 → 後 |
|---|---|---|---|
| 集落 | 96 → 96 | 141.1 万 → 142.9 万 | 12,343 → 14,229 |
| 船台 | 99 → 99 | 109.1 万 → 110.1 万 | 9,518 → 10,875 |
| 群れ | 87 → 87 | 118.6 万 → 122.6 万 | 11,504 → 13,905 |
| 林 | 98〜102 → 98 | 105.0 万 → 110.4 万 | 9,355 → 11,888 |
| 狼 | 104 → 105 | 107.4 万 → 110.2 万 | 10,367 → 12,242 |
| 海岸 | 82 → 82 | 124.0 万 → 129.5 万 | 12,049 → 14,918 |

draw call は増えません(草は 1 回のまま)。三角形は最大の集落で 142.9 万(上限 150 万)です。
fps: 別の作業の観察画面が同じ GPU で動いていた間は、前も後も集落で 42fps(同じ条件で続けて測った値)でした。ほかの画面が止まっていた間は、後の集落で 75fps(画面の上限)、兎に寄った画で 70fps でした。

基準画(`sheets/flora.png`・`sheets/terrain.png`・`key-visuals/herd.png`)と比べて届いていないところ:

- 近くの房はまだ 1 つずつの房に見えます。基準画の「茂った草地」は房の境目の無い絨毯で、葉の数が 1 桁多いです。房の数を増やすと三角形の上限(集落で残り 7 万)に当たります。
- 集落の引きでは、本体の草の密度が低いので房が少なく、平らな芝に見えます。岩・木の根元・石垣の際に房を寄せる置き方はしていません(木と集落の置き場所が草より後に決まるため)。
- 基準画の森の床(落ち葉・根)や、「踏まれて土の出た草地」の足跡の凹凸はありません。色の斑だけです。
- 葉は板で、先の毛羽立ち(穂)や光の透け(逆光で葉が光る)はありません。
- 筆のむらは空気の層(霞)の下では弱く見えます。近くの草地にしか効いていません。

## 追記(木の磨き上げ)

審査台への判断(「草や木をひっとうにまだ磨き上げる余地はある」)のうち、鐘樹の成木と森の木を直しました。草は別の作業(上の「追記(草の磨き上げ)」)です。
比較画は上が前、下が後です: `trees-before-after-grove.png`(林の寄せ先)・`trees-before-after-groveTrack.png`(自動カメラの林の横移動、開いてから約 100 秒 / 50 秒の最初の groveTrack の 3 秒目)・`trees-before-after-settlementHigh.png`(自動カメラの集落の俯瞰、最初の settlementHigh の 4 秒目。ゆっくり回るので前と後で向きが少しずれます)。
撮り方は `observe.html?time=0.12&freeze=1&ship=60&shot=林`、自動カメラは `shot` を付けずに開き、`__observeStats.camera` がそのショットになるのを待ちました。
`trees-vs-reference.png` は上から基準画 `sheets/belltree.png`、前の並べ図 `belltree.png`、後の並べ図(芽・若木・成木・株・丸太)、前の `flora2.png`、後の `flora2.png` です。

直したところ:

- 葉のカード: 樹冠の塊の表面に、葉の房を描いた絵のカード(四角 2 三角形)を散らしました(`observe_kit.scatter_cards`)。
  絵は `observe_kit.leaf_card_image` が手続きで描く 1024 × 1024 の 2 × 2 の区画で、外へ向いた卵形の葉 26〜32 枚の房です(`assets/textures/observe/leaf_card.png`、外部の素材は使っていません)。
  絵の色は白に近い明るさの揺らぎ(葉ごとの明暗、暖色と寒色の揺らぎ、葉脈、縁の陰り)だけで、葉の色は頂点色が持ちます。
  カードの法線は房の中心から外向きに揃え(柔らかい法線 1.0)、房ごとの丸い量感で陰ります。
- 樹冠の明暗: カードは房の上ほど明るく暖かく(1.18 倍、青を抜く)、下ほど暗く寒色に(0.6 倍)。塊は 0.86 倍に縮め、陰りに (0.5, 0.56, 0.7) を掛けてカードの奥の暗い内側にしました。
  塊は輪郭をカードが作るので粗くし(成木は房の真ん中の塊だけ細かさ 1、lod1 は全部 0)、その三角形をカードに回しました。
- 房の間の隙間: 外の端が別の房の塊から 0.35 m(森の木は 0.25 m)以内に届くカードは置きません。試作 3 の判断の「光の筋が出る程度の狭い隙間」を残すためです。
  日が抜ける割合(`canopy-gaps`、カードは光線が当たった所の絵のアルファで数え、包みにはカードの面の中心を入れる): 成木 0.08〜0.16、lod1 0.08〜0.15、森の木 0.08〜0.13、森の木 lod1 0.07〜0.15(前は成木 0.10〜0.15、lod1 0.08〜0.16)。
  隙間を塞ぐカードを除く前は 0.07〜0.12 まで下がり、`canopy-gaps.png` の真上からの影で房の間が埋まっていました。
- 鐘: 数と位置はそのままで、付け根を最も近い塊の中心から 1.28 倍外へ出し、付け根から 0.5 m 以内にはカードを置きません(葉に埋もれない)。
- 幹: 成木は 14 面(lod1 8 面)で、1 つおきの頂点を 6〜13% 凹ませた縦の裂け目と、根元 2 輪の張り出し。裂け目は暗く、筋の濃淡は角度でゆっくり揺らし、根元 0.2〜1.5 m に苔の緑を頂点色で載せます(幹と根の材質 `belltree_trunk` は白で、樹皮と苔の色は頂点色)。
  板根は 6 本(lod1 4 本)で、断面を縦長の楕円(横 0.62・縦 1.35)の鰭にしました。森の木も同じ作りです(茶色の幹、12 面、根 4 本の鰭、葉は濃く青い緑)。
- 観察画面(`src/observe/render/foliage.ts`): 切り抜き(glTF の alphaMode MASK)の材質は葉のカードの材質にします。両面で、裏から見ても法線を裏返しません(裏返すと房の内向きの法線になり、カードごとに明暗がまだらになる)。
  縁の光は 0.06 に弱めました(外向きの法線で輪郭のカードが一斉に白く光り、葉が褪せて見えた)。
  alphaToCoverage は試して外しました。縁の画素のアルファが 1 未満のまま画面まで届き、キャンバス(alpha: true)の後ろが白い点々として透けたためです。
  影は絵のアルファで切り抜く深度の材質で落とし(`foliageDepth`)、インスタンスにも引き継ぎます。カードは光線の当たり判定(自動カメラの見通し)から外し、奥の塊が受け持ちます。
  焼き(`bake.ts`)は絵つきの材質を焼かないので、カードは木の種類と LOD ごとに別の draw call(本描画と影)になります。
- Blender の確かめ(`observe_render.py`): 絵を持つ材質は、絵の色を掛け、絵のアルファ 0.5 で切り抜き、裏の法線を裏返さず、縁の光を弱く描きます。GLB は `export_glb(..., texcoords=True)` で UV を書き出します(上の「既知の課題」の UV は、葉のカードについては解消)。

三角形(1 本): 成木 3,646(前 3,917、予算 4,000)、lod1 1,080(1,112、1,200)、森の木 1,288(1,625、2,000)、lod1 380(575、600)。GLB は belltree 672 KB(210 KB)、flora 519 KB(101 KB)で、どちらにも同じ絵が 1 枚入ります。

予算(1600 × 900、`__observeStats`、6 つの寄せ先 集落/船台/群れ/林/狼/海岸):

| | draw call | 三角形 | fps |
| --- | --- | --- | --- |
| 前(草の磨き上げの前の本体) | 96 / 99 / 91 / 98 / 101 / 83 | 141 / 109 / 126 / 105 / 112 / 124 万 | 47〜50 |
| 後(木だけ) | 102 / 104 / 94 / 104 / 112 / 87 | 138 / 108 / 120 / 104 / 112 / 122 万 | 52〜56 |
| 後(草の磨き上げを取り込んだ後) | 102 / 104 / 93 / 104 / 108 / 87 | 140 / 109 / 120 / 110 / 110 / 128 万 | 57〜59 |

fps は別の作業の画面と GPU を分け合った測りで、同じ条件で前と後を入れ替えて測っても揺れの範囲(前 47〜50 と後 52〜56 は数分違いの測り)です。落ちたとは言えませんが、上がったとも言えません。
三角形は影の描画を含みます。成木の三角形を予算の中に収めたので、集落でも 140 万に収まります(カードを足しただけの途中の版は 154 万で超えた)。

自己評価(基準画 `sheets/belltree.png`・`key-visuals/herd.png` と比べて):

- 近づいたところ: 樹冠の輪郭と面の中が葉の縁になり、滑らかな黄緑の塊(刈り込んだ木)には見えなくなりました。引き(集落の俯瞰)でも葉の房の粒が読めます。
  房の上が明るく下と奥が暗い明暗、淡い幹の縦の筋、鰭の板根は基準画の方向です。地面の影にも葉の縁と小さな抜けが出ます。
- 届いていないところ:
  - 房の塊が今も一つずつ丸く読めます。基準画の樹冠は小さな房が重なって一つの大きな丸い樹冠になっていますが、こちらは房の間の隙間(光の筋のため)を残したので、房 8 つ(森の木は 6 つ)の輪郭がそのまま見えます。
  - 近く(10 m 以内)では、カードが寝た向きで細い線に見える所と、カードの切れ目から粗い塊(20 三角形)の角が覗く所があります。
  - 葉の絵は 1 種類の卵形で、基準画の葉の大きさの揃い方・房の中の筆の跡までは出ていません。風の揺れもありません。
  - 苔は頂点色だけで、基準画の根元の苔の盛り上がりと、根元の地面の草むらはありません。樹皮の筋は頂点の間隔(14 面)で、細い裂け目は描けません。
  - 若木・芽・株・丸太は変えていません。`shipyard.png` は描き直していません(樹冠が前のまま)。
  - 縁の光を弱めたので、逆光の縁の明るさは前より控えめです。

## 追記(集落の建物の作り直し)

審査台への判断(2026-09-24「船の造形は大幅によくなった。集落の建物も最終的にはこの水準を超えたい」)で、集落の建物を作り直しました。
舟の造形(`ship.png`)が良く見える理由を、30〜50 m で読める輪郭、どう組んだかが見える層(板・肋・索具・鐘・船首の渦)、明暗の揺らぎのある暖かい材、小さな暮らしの話の 4 つと読み、同じことを小屋と小物に当てました。
途中で段取りが変わり(2026-09-24、ユーザー)、三角形を減らす作業(遠景の LOD・描く範囲の間引き)は別の段で行うことになりました。ここでは見た目を優先し、三角形の数は測って残すだけにしています。

比較画:

- `settlement-before-after-high.png`: 上が前、下が後。自動カメラの集落の俯瞰(`observe.html?time=0.3&freeze=1&ship=80`、最初の settlementHigh の 4 秒目)。ゆっくり回るので前と後で向きが少しずれます。
- `settlement-before-after-close.png`: 西の小屋(集落の中心から (−14, −8))に、南東から 13 m・高さ 4.5 m の斜めの寄り(`__observeAir` でカメラを置いた。前と後で同じカメラ)。
- `settlement-night.png`: 夜(`time=0.8`)。上から前の引き、後の引き(広場の東から西の小屋へ)、後の寄り(上と同じ 13 m の寄り)。
- `settlement-vs-reference.png`: 上が基準画 `sheets/settlement.png` の切り出し、下が作り直した部品を同じ並び(小屋・灯り柱・L 字の石垣・衝立)で描いた `settlement-pieces.png`。
- `settlement-ship.png`: 完成の舟(船台に載せた `ship_sails`)の前に、作り直した部品と月鹿を並べた大きさと作り込みの比べ。左端は遠景用の `hut_lod1`(ゲームでは使っていない)。
- `settlement-hut.png`: 小屋の寄り(戸口の側から)と、奥に `hut_lod1`。

撮り直しの手順:

```sh
blender -b --factory-startup --python tools/blender/observe_settlement.py
blender -b --factory-startup --python tools/blender/observe_render.py -- verify assets/models/observe/settlement.glb
blender -b --factory-startup --python tools/blender/observe_render.py -- pieces docs/design/qa/observe/settlement-pieces.png
blender -b --factory-startup --python tools/blender/observe_render.py -- settlement-ship docs/design/qa/observe/settlement-ship.png
blender -b --factory-startup --python tools/blender/observe_render.py -- hut docs/design/qa/observe/settlement-hut.png
```

直したところ(`tools/blender/observe_settlement.py`):

- 小屋 `hut`:
  - 屋根: 棟 3.35 → 4.3 m、軒の出 2.15 → 2.55 m の急な切妻にし、棟と屋根面の中ほどを 0.1 m 垂らしました。
    下地の板の上に、手で割った屋根板(長さ 1.18 m、幅 0.45〜0.8 m、軒の側の端はぎざぎざに欠ける)を軒から 5 段重ねます。
    板の縁は暗く(0.5 倍)、段ごとに影の線が出ます。これは舟の鎧張りの外板と同じ読ませ方です。
    板の色は地の色(#8C6242)から少しだけ揺らし、上の段ほど雨に晒された灰に、軒に近い段ほど苔に寄せます。
    ときどき 1 枚が欠け(下地が覗く)、ときどき 1 枚が反り上がります。
  - 組み: 四隅の巨石の柱に石の笠を載せ、その上に丸太の桁(前後)と梁(左右、前の梁は戸口の鴨居)を渡し、柱の上で縄で括りました。
    垂木は片面 5 本で、軒から尾が覗きます。軒の下の縁は鼻の丸太で締めます(舟の腰板と同じく一本の線で輪郭を締める)。
  - 妻: 交差した竿の先を、外へ 1 巻き余り巻いた渦にしました。舟の船首の渦と同じ手の彫りで、集落と舟を同じ民が作ったと読めるようにしています。
    交差は縄で括り、奥の妻は縦に割った板で塞ぎ、前の妻は開けて棟から吊り灯籠を下げます。
  - 壁: 一つずつの石の空積みで、奥・左右は 4 段(下ほど大きく厚い)、戸口の両脇は 3 段。石の長さはばらし、段ごとに目地がずれます。上の段に苔が載ります。
    石は白の材質 `settlement_stone_paint` に色ごと頂点色で載せ、石ごとに暖かい灰と冷たい灰の間で揺らし、地面から 0.45 m まで苔が這い上がります。
    軒の下と小屋の中は奥ほど・低いほど暗くしました(`hut_ao`)。
  - 暮らし: 戸口の敷居、床の敷き藁と敷物、炉(石の輪・光る熾火・焦げた薪)、壺、籠 3 つ、右の壁の外に薪の山(切り口が明るい)、左の壁の外に干し棚と干した草の束、鴨居に鐘樹の青銅の鐘 2 つ(口が暖かく光る)。
  - 蔓: 3 本が柱を登って屋根を越え、軒から垂れます。点の間に横の揺れを入れてうねらせ、葉を 3 枚の房で付け、細い巻きひげを付けました。左の軒から短い蔓が 2 本垂れます。
  - ムーの意匠: 戸口の左の柱に刻んだ六角の紋だけが淡いシアンに光ります(巨石・月鹿の継ぎ目と同じ)。
  - 斜面の土台: 壁と柱の下に 1 m の石を埋めました。ゲーム側は小屋を戸口の外の地面に合わせて上げる(下の「置き方」)ので、下り側に土台が覗きます。
- 遠景用の `hut_lod1`: 元の `hut` の関数を、作り直した小屋と同じ輪郭(柱・3 段の壁・屋根板の段・棟・妻の竿・炉と灯籠の光・薪・土台)に組み替えたもの(766 三角形)。
  三角形を減らす段で使う前提で用意しただけで、ゲーム側では切り替えていません。妻の渦・蔓の葉・小物はありません。
- 敷石 `stepping_stone`(新規): 戸口から広場へ下りる平たい石 1 枚。斜面に沿うよう、ゲーム側が 1 枚ずつ地面に置きます。
- 灯り柱 `lantern_post`: 柱を 0.44 × 0.4 × 2.3 → 0.64 × 0.56 × 2.75 m に太くし、頭を斜めに割り、上に彫った帯を付けました。
  腕木は太い石にし、先に下へ出た石の鼻、根元に縄を巻き、鼻から吊り紐。灯籠は基準画どおり四角(四角錐の笠・光る胴・四隅の柱・台・吊り輪)で、四面に X の木の格子です(六角の胴はやめました)。
- 石垣 `stone_wall`: 一つずつの石の 4 段(下は大きく、上は平たい笠石に苔)と端の太い立石、足元に落ちた石 2 つ。
- L 字の石垣 `stone_wall_corner`: 腕を 4 段にし(腕 B は崩れかけて上の段が短い)、端の立石を 1.05 m にしました。
  紋は彫り込んだ四角の枠の中の六角と、その上下の短い溝です(基準画の端の石の刻み。字に見えないよう線は枠と紋だけにし、光らせない)。
- 衝立 `woven_screen`: 柱は 3 本の枝の束を 3 か所で縄で括り、帯は横 9・縦 11 の細い繊維を隙間を残して編みました。
  蔓を 2 本斜めに絡ませ、上の横木に小さな鐘を 5 つ、両の柱に 1 つずつ下げます。
- 船台 `slipway` と巨石 `megalith` は変えていません(舟の置き方が船台の寸法に依っているため)。

置き方(`src/observe/settlementLayout.ts`、`src/observe/view.ts`):

- 小屋は戸口を広場へ向けます(前は 0.4 / −0.6 / 0.1 の決め打ちの向き)。広場から小屋へ踏み固めた道が、戸口の敷石で終わります。
- 集落の小屋は斜面に立ちます(西の小屋の足元は 5 m の間に 2.3 m の高低差)。前は中心の高さに置いていたので、戸口の側が上り坂の小屋は床と戸口が地面に埋まりました。
  中心と戸口の外(1.8 m)の地面の高い方に合わせ、中心から 0.8 m までの範囲で上げます。
- 敷石は戸口の外 2.1 / 2.75 / 3.4 m に、1 枚ずつ地面の高さに置きます。
- 小屋の炉にも夜の灯りの溜まり(`motes.ts` の灯りと同じ、帆を失うと灯りと一緒に消える)を置きました。夜は戸口から火の明かりがこぼれ、小屋の中の床が暖かく照ります。
- 単体試験: `tests/unit/observe.settlementLayout.test.ts`(戸口が広場を向く、炉の位置、平地・上り坂・下り坂・急な坂の高さ、敷石の並び)。

三角形(1 つ):

| ノード | 前 | 後 | 備考 |
| --- | --- | --- | --- |
| `hut` | 1,934 | 10,178 | 大半は屋根板・一つずつの石・蔓の葉 |
| `hut_lod1` | — | 766 | 新規(ゲーム側は未使用) |
| `stepping_stone` | — | 44 | 新規(小屋 1 つに 3 枚) |
| `lantern_post` | 512 | 586 | |
| `stone_wall` | 440 | 948 | |
| `stone_wall_corner` | 680 | 1,544 | |
| `woven_screen` | 360 | 1,060 | |
| `slipway` / `megalith` | 1,982 / 596 | 1,982 / 596 | 変えていない |

GLB は 470 KB → 1.07 MB です。ゲームの集落には小屋 3・灯り柱 5・石垣 2・L 字 1・衝立 2・敷石 9 を置くので、影の描画を含めて集落の寄せ先で約 5.8 万三角形増えます。

予算(1600 × 900、`__observeStats`、`observe.html?time=0.12&freeze=1&ship=60&shot=集落` を開き、ページの寄せ先の押しボタンを順に押して 4 秒後から 0.5 秒おきに 8 回):

| 寄せ先 | draw call 前 → 後 | 三角形 前 → 後 |
| --- | --- | --- |
| 集落 | 102 → 107 | 139.7 万 → 145.5 万 |
| 船台 | 103〜105 → 108 | 108.5〜108.8 万 → 114.4 万 |
| 群れ | 107 → 114 | 99.1〜99.3 万 → 105.3 万 |
| 林 | 106 → 110 | 109.9 万 → 115.4 万 |
| 狼 | 108〜113 → 117 | 108.0〜112.5 万 → 117.6 万 |
| 海岸 | 87 → 88 | 127.6 万 → 130.4 万 |

前は同じ worktree で `settlement.glb` と `view.ts` だけを作り直しの前(db4a3eb)に戻して測り、後は最後の版です。群れ・狼は動物が動くので回ごとに揺れます。
集落の寄せ先の draw call の増えは 5 つです: 敷石の本描画と影、炉の灯りの溜まり 3 つ。小屋の敷物を両面の材質にすると焼きの両面の組が増える(本描画と影で 2 つ)ので、片面の材質にしました。
三角形は予算 150 万の中に収まりました(集落 145.5 万が最も多い)。遠景の LOD と描く範囲の間引きは三角形を減らす段で行います(`hut_lod1` を用意してあります)。

fps は、別の作業の観察画面(5299 番のページ)と GPU を分け合った測りで、前も後も 1 回の測りの中で 34〜35 と 64〜68 の 2 つの値を行き来しました(75 の半分の刻み)。
前の 2 回は 34〜67、後の 3 回は 34〜68 で、同じ条件で前と後を比べられていません。落ちたとも上がったとも言えません(未確認)。

自己評価:

- 基準画(`sheets/settlement.png`)と比べて:
  - 近づいたところ: 小屋は、四隅の巨石の柱と低い空積みの壁の上に、手で割った板の屋根が深い軒で被さる形になり、屋根の段・ぎざぎざの板の端・うねる太い蔓・苔の載った石の段が読めます。
    灯り柱は太い石の柱・石の腕木・四角の X の格子の灯籠という基準画の組み立てどおりです。L 字の石垣は端の石に彫った枠と紋が付きました。衝立は束ねた柱・縄の括り・細かい編み目・小さな鐘です。
  - 届いていないところ:
    - 屋根板は平らな板で、基準画の板の割れ・木目・反りの筆の跡はありません。板の数は基準画より多く、一枚ずつは基準画より小さく見えます。
    - 柱は角を落とした箱で、基準画の柱の丸い頭と苔の筋(縦に垂れる苔)はありません。石の苔は根元の這い上がりと上面だけです。
    - 蔓は太さが一定に近く、基準画の蔓の絡み(梁に巻き付く、屋根の縁から垂れる大きな輪)には届いていません。葉は 1 種類です。
    - 石垣は 4 段の石ですが、基準画の石の細かさ(5〜6 段の小さな石)と、根元の苔の盛り上がりはありません。紋は字に見えないよう線を減らしたので、基準画の刻みより簡素です。
    - 衝立の編み目は規則正しく、基準画の手編みの不揃い・ほつれ・斜めの蔓の多さはありません。
    - 地面との接し方(根元の草むら・苔の盛り上がり)はありません。
- 舟(`ship.png`)と比べて:
  - 並んだところ: 30〜50 m の引き(`settlement-before-after-high.png`)で、屋根の段と妻の渦と蔓が読め、前の平らな板の屋根よりはっきり「組んで作った家」に見えます。
    妻の渦は舟の船首の渦と同じ意匠で、舟の鐘と同じ青銅の鐘・同じ灯籠の暖かい光を持たせました。夜は炉と灯籠で、舟の舷の鐘の列と同じく集落が灯りで読めます。
  - 超えていないところ: 舟の強さは大きな一つの形(深く丸い船体と高く巻いた船首)と、規則正しく細かい線(外板の段・索具)の対比にあります。小屋は小物が多いぶん、引きでは形が舟ほど一つにまとまらず、色も屋根の茶・石の灰・蔓の緑に分かれます。
    舟の水準を「超えた」とは言えず、近い所まで来た、というのが私の評価です。超えるには、屋根の輪郭(軒の反り・棟の渦)をもう一段強くし、引きで一つの形に読める量感(例えば屋根を地面近くまで下ろす、基準画 `key-visuals/shipyard.png` の石の家)を試すのが次の手です。
- 未確認: fps の前後の比較(上)。`shipyard.png`・`corner.png`・`settlement.png`(前の並べ図)は描き直していません。ゲーム内の画は小屋の敷物を片面の材質に替える前の版で撮りました(敷物の色が少し違うだけです)。
