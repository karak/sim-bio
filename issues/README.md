# issues — チケット管理

ツールを使わず、このフォルダの Markdown ファイルでチケットを管理する。

## 規約

- 1 チケット = 1 ファイル。ファイル名は `<milestone>-<連番>-<slug>.md`(例: `M1-07-world.md`)。
- 先頭の frontmatter で状態を持つ。

```yaml
---
id: M1-07
title: World
status: todo        # todo | in_progress | blocked | review | done
milestone: M1
plan: docs/specs/plans/2026-09-19-m1-implementation-plan.md#task-7-world
depends_on: [M1-03, M1-04, M1-05, M1-06]
evidence: []        # done 時に commit SHA とテストファイルを列挙
---
```

- 状態遷移は frontmatter の `status` を書き換える。着手時 `in_progress`、テスト通過とコミット後 `done`。
- `done` にする時は `evidence` に commit SHA とテストファイルパスを必ず入れる(受入基準の証跡)。
- 一覧は `tools/issues.sh` で表示する。

```bash
tools/issues.sh          # 全チケットを状態付きで一覧
tools/issues.sh todo     # 状態で絞り込み
```

## シナリオの作り方(レベルデザイン → 実装 → 校正)

シナリオが数値上成立しないとき、閾値や係数を動かして通すのは「完走の成立」であって「体験の成立」ではない。
シナリオを含むマイルストーンでは、校正チケットの **前** に必ずレベルデザインのチケットを置く。

1. **レベルデザイン**(`docs/design/<date>-level-design-<scenario>.md`): 体験の芯(何で悩ませるか)、キーアイテム
   (舞台装置・生物の特性を 1〜2 個)、ループ、判定行列。判定行列には「レバー感度」(各レバーが目的変数を動かす)と
   「定着」(放流が残る)と「副作用」(レバーの代償)の確認を含める。
2. **キーアイテムの実装**: レベルデザインで決めた仕組みを縦切りで入れる。
3. **校正**: 感度・定着・副作用がヘッドレスで通ってから数値を合わせ、放置 dead・素朴戦略 dead・配分 alive を tests/slow で固定する。
   感度が通らなければ係数ではなく仕組みに戻る(1 へ)。
4. **手動受入**: ビルド済みで 3 回遊び、記録する。

判定行列は **UI と同じ手**(放流 環 1・0.5、力 4。災害の半径は UI の上限内)で書き、手で勝てない想定解は成立と数えない。
ScenarioRunner の放流の値段は半径に依らないので、環 3〜4 の台本は UI の 10 倍の量を同じ値段で放ち、手で再現できない想定解になる。
M11 以降は各マイルストーンの先頭に `-00 レベルデザイン` チケットを置き、実装と校正のチケットはそれに依存する。

## 進める順(2026-09-23 承認、docs/specs/plans/2026-09-23-roadmap.md §7)

| 順 | マイルストーン | 備考 |
|---|---|---|
| 0 | **M22 観察画面(空の舟)** | 2026-09-23 20:00 に最優先で追加。M22-01 ボード → M22-02 デフォルメ試作 → 残り。決定は docs/design/2026-09-23-observation-view.md |
| 0b | **M23 観察画面の軽量化** | 2026-09-24 08:30 にユーザーが別フェーズとして追加。M23-01 計測の台 → 02 視錐台カリング → 03 海 → 04 影 → 05 草の遠距離版 → 06 木のインポスター → 07 塗りの負荷 → 08 動物の遠距離版。調べと試算は docs/design/2026-09-24-observe-perf.md。チケットごとに worktree を分ける |
| 1 | M21 仕上げ | M21-01・02 済。M21-03 迎撃の作り直し(試作の案あり)と M19-03 回帰の通し実行は M22 の後 |
| 2 | M19 公開(Cloudflare 無料枠) | M21 の直後(遊べる URL)。設計は docs/design/2026-09-26-cloudflare-architecture.md。順: M19-04 決定論の刻み → 05 手元の保存・06 年代記 → 07 港の契約 → 01 LogSink・02 配信・12 構成検査 → 08 港の Worker → 09 港のクライアントと UI → 10 積荷 → 11 回避率。M19-04 は観察画面と独立に先に進めてよい |
| 3 | M12 気候の帰還 | #4・#10・#11・#12(M12-06 氷期を追加) |
| 4 | M16 疫病と流入 | |
| 5 | M11 星砂と石喰い | |
| 6 | M14 持ち込みと外来種 + 歌鳥 | M17-04 歌鳥はここ |
| 7 | M13 霊脈と熱 | 未チケットの #3・#24 の扱いを M13-00 で決める |
| 8 | M15 地形と津波 | |
| 9 | M17 伝説の生き物 | 夢喰いを種にするかは M17-00 で判断 |
| 10 | M18 予言と琥珀 | |
| 随時 | M19-01 HTTP LogSink | |

チケットの番号は付け替えない。別のマイルストーンへ移したチケットは id を保ち、frontmatter の `milestone` だけを書き換える
(M17-04 歌鳥 → M14、M19-03 回帰の通し実行 → M21)。
