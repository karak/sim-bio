# 観察画面の灰狼(M22-05)

基準画 `assets/textures/board/creatures/wolf.png`(承認済み)を、月鹿(`deer.md`)と同じ作り方・同じ密度(丸めた形・なめらかな陰影)で起こした灰狼。
造形の元は `assets/textures/concept/wolf-angular.png`。画風が食い違ったら動物側の基準画を正とする(`assets/textures/board/README.md`)。

## 出力と作り直し方

| もの | 場所 |
|---|---|
| モデル | `assets/models/observe/wolf.glb`(+ `wolf.blend`) |
| 組み立て | `tools/blender/observe_wolf.py`(`observe_deer.py` の部品・リグ・IK・焼き・書き出しを引き継ぐ) |
| 撮影 | `tools/blender/observe_wolf_render.py`(鹿と同じ光・紙色の背景・グレア) |
| 比較画 | `tools/blender/observe_wolf_sheet.py`(python3 + Pillow) |
| 書き出しの検証 | `tools/blender/observe_wolf_check.py`(GLB を Blender に読み戻して数える) |

```sh
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_wolf.py -- assets/models/observe
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_wolf_render.py -- assets/models/observe/wolf.blend <raw_dir> 720
python3 tools/blender/observe_wolf_sheet.py <raw_dir> docs/design/qa/observe
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_wolf_check.py -- assets/models/observe/wolf.glb
# 接地・ループの継ぎ目まで見るときは種ごとの一般化版(狼にも使える)
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_creature_check.py -- assets/models/observe/wolf.glb
```

撮影の 4 つ目の引数に `views` を渡すと、比較画に使う `view-*`・`pose-*` だけを撮る(作り直しの途中の確認用)。

鹿のスクリプトは変えていない(汎用化せず、狼用に写して直した)。

## 実測(`observe_wolf_check.py`、GLB を読み戻した値)

| メッシュ(ノード名) | 三角形 | 頂点 | 内訳(材質別の三角形) |
|---|---|---|---|
| `wolf`(近 LOD) | **2,639** | 1,689 | body 1,402 / dark 664 / glow 480 / nose 66 / pale 27 |
| `wolf_lod1`(群れ LOD) | **690** | 479 | body 400 / dark 172 / glow 90 / nose 28 |

- 予算(近 ≈2,500 / 群れ ≈650)に対し 近 +6%、群れ +6%(+10% の内)。群れ LOD は VAT に焼くので頂点を 479 に抑えた。
  (手直し前は 近 2,541 / 1,609 頂点、群れ 650 / 455 頂点)
- 寸法: 単位 m。背の線(肩)0.85 m(組み立ての断面の値)、全高 0.93 m(肩の棘の先・耳の先、bbox)、鼻先から尾の先 1.56 m(y -0.88..0.68)、幅 0.38 m。足の底が z = 0、前後の足の中点が原点(手直しで前足を 4 cm 前、後足を 2 cm 後ろへ出したので、中点は 1 cm 前)。
- 全アクションで近 LOD の一番低い頂点は z = 0(`observe_creature_check.py`。手直し前は fall -0.204 m・pounce -0.027 m・stalk -0.015 m・walk -0.003 m 地面へ潜っていた)。ループの最初と最後の姿勢の差は 0.0000。
- 向き: Blender で正面 -Y → glTF で **+Z が正面、Y が上**。
- 2 つのメッシュは同じスキン(`wolf_rig`、骨 29 本)を共有し、どちらもシーンのルートに置く(鹿と同じ)。
- 1 頂点あたりの影響は最大 3 本、重みの合計はすべて 1。頂点色 `COLOR_0` は全プリミティブに付く(毛以外は白)。
- glTF-Validator(npm `gltf-validator` 2.x): **errors 0 / warnings 0 / infos 0 / hints 0**、アニメ 6、材質 5、スキンあり(手直し後に再確認)。

## 骨(29 本)

`root`(動かさない)・`pelvis`・`spine1`・`chest`・`neck1`・`neck2`・`head`・`jaw`・`ear_L/R`・`tail1/2/3`、
脚 4 本 × 4(前: `fl_upper/fore/meta/paw_{L,R}`、後: `hl_thigh/shin/meta/paw_{L,R}`)。目・棘・飾り毛は頭か胴・首に固定。
どの骨もローカル X がワールド X を向く(鹿と同じ)。前向きの骨は X 回りの正で先が下がり、後ろ向きの尾は正で先が上がる。

## アニメーション(30 fps、その場。root は全フレームで 0)

| 名前 | 長さ | ループ | 中身 |
|---|---|---|---|
| `idle` | 4.0 s(120 f) | する | 呼吸(2 回)、首をゆっくり左右、1.0 s に左耳・2.7 s に右耳をはじく、尾のゆるい揺れと 2.0 s の一振り |
| `walk` | 1.1 s(33 f) | する | 4 拍の常歩(左後 → 左前 → 右後 → 右前、立脚 62%、歩幅 0.40 m) |
| `stalk` | 1.6 s(48 f) | する | 体を 0.10 m 沈め、胸を下げ、首を前へ伸ばして頭を水平に保つ忍び足(立脚 76%、歩幅 0.30 m)。耳は前 |
| `run` | 0.5 s(15 f) | する | 回転ギャロップ(右後 → 左後 → 右前 → 左前、立脚 34%、歩幅 0.72 m)。背を曲げ伸ばし、耳を伏せ、尾を後ろへ伸ばす |
| `pounce` | 1.2 s(36 f) | しない | 0〜0.35 s 後ろへ沈んで溜める、0.35〜0.6 s 前下へ 0.30 m 飛び込み前足を 0.45 m 先へ(顎を開く)、0.75〜1.2 s 前足・後足を一歩ずつ戻して rest に戻る |
| `fall` | 2.0 s(60 f) | しない | 0〜0.6 s 前脚が折れる、0.45〜1.4 s 横倒し、1.2〜2 s 首と頭が地に落ちて止まる。倒れ始めまでは足を地面に残して(IK)脚が折れ、各フレームで一番低い頂点を地面に合わせる(土兎と同じ) |

- 脚は 2D の解析 IK(肩・股関節から足の付け根)で、立脚中は足が地面に固定される。ループはどれも最初と最後のフレームが同じ。
- `pounce` は骨盤が一時的に前へ 0.37 m 動く(その場の原点は動かさない)。最後のフレームは rest と同じなので、終わったら idle / walk へそのまま戻せる。捕まえた後の移動は Three.js 側で個体を動かす。
- `pounce`・`fall` は Three.js で `LoopOnce` + `clampWhenFinished`。

## 材質

| 名前 | 色(sRGB) | 使い方 |
|---|---|---|
| `wolf_body` | 頂点色(地 #CC6E40、上を向く面 #E49C5C、脇の下側・腿の裏・尾の下の陰 #B2623A、喉・胸・腹・鼻づらの下・頬 #A8856C、耳の内側 #8E4E36)、baseColorFactor 1 | 毛。淡い色と、焦げ茶の手前の暗み(脚)は頂点色 |
| `wolf_dark` | #6B412F | 脚の下(前脚は肘の少し上、後脚は膝の少し上から)と足 |
| `wolf_pale` | #A8856C | 頬と胸の飾り毛(尖った楔、近 LOD のみ)。淡い面そのものは `wolf_body` の頂点色 |
| `wolf_nose` | #3A2826 | 鼻(鼻先に載せた小さな塊)、目の縁 |
| `wolf_glow` | #8FF5E6、emissive #8FF5E6(係数 1) | 背の稜線(頭の後ろ → 尾の先)、肩と首の上の棘の前の面、肩の後ろ・肩の前(胸の前まで下ろして前へ回す)・腰の前・尻の後ろの継ぎ目、眉(近 LOD)、目 |

- 全材質 roughness 0.8、metallic 0、specular 0。陰影は Three.js 側のトゥーンのランプで付ける前提(鹿と同じ)。

## 比較画

上から 基準画の上段(側面・正面・斜め前)、モデル(rest、同じ向き)、基準画の下段(忍び寄り・疾走)、モデル(`stalk` 12 f・`run` 12 f、側面)。

![比較](wolf-compare.png)

| 画像 | 中身 |
|---|---|
| `wolf-compare.png` | 上のとおり 5 体ずつ |
| `wolf-view-{side,front,q34,rear34}.png` | モデル単体 512² |
| `wolf-lod.png` | 近 LOD / 群れ LOD(側面・斜め前) |
| `wolf-anim.png` | idle(1.1 s)・walk(8 f)・stalk(12 f)・run(12 f)・pounce(17 f、飛び込み)・fall(最後) |
| `wolf-seq-{walk,stalk,run,pounce,fall}.png` | 側面の連続 |

![LOD](wolf-lod.png)
![アニメ](wolf-anim.png)

## 作り直しの記録

1. **1 回目**: 基準画の側面を 0.0029 m/px で測って胴・首・頭・脚・尾を組んだ。頭が長く尖り(狐)、耳が小さく、継ぎ目が胴を一周する縞に見え、淡い胸がほとんど見えず、焦げ茶が脚の下だけ(長靴)。run で尾が腹の下へ回った(後ろ向きの骨の回転の符号の取り違え)。
2. **2 回目**: 頭を短く太く、耳を大きく、焦げ茶を肘・膝の少し上から、淡い色を喉・胸・頬へ広げた。継ぎ目を短く、足を大きく。尾・耳の回転の符号を直した。
3. **3 回目**: 頭を 5 cm 持ち上げた(基準画の側面は頭が低く、正面・斜め前は高い。rest はその間にして、stalk で首を下げる)。肩幅を広げ(正面で頭より胴が狭かった)、棘を首の後ろから肩へ寄せた(正面で頭の上に突き出た)。
4. **4 回目**: 鼻づらの先を丸く太く(尖りを減らす)、肩の継ぎ目を短く。fall の首の横曲げの向きを直した(頭が地面から持ち上がっていた)。疾走の比較は脚を体の下に集めた 12 f にした。
5. **5 回目(M22-05 残りの手直し、試作 2 への判断 5)**: 下の「手直しの前と後」のとおり。

## 手直しの前と後(M22-05 残り、2026-09-24)

ユーザーの判断(試作 2 への判断 5「優先順位はあとだが、やってほしい」)で、狼寄りの造形・塗り・光・姿勢を詰めた。

| 項目 | 前 | 後 |
|---|---|---|
| 鼻づら | 細く尖る(先の断面 3.6 × 3.2 cm の円錐)。鼻先全体が鼻の材質で、正面から大きな黒い玉 | 楔のまま先を太く(幅 9 cm・深さ 8.6 cm)、断面を箱寄り(`head_sq`、2.2 → 2.7)。頬を広げ、鼻先を 3 cm 下げて頭を前下がりに。鼻は鼻先の前・上に載せた小さな塊(`build_nose`、幅 4.6 cm) |
| 胴 | 超楕円の断面(樽) | 背の面・脇の面・腹の面を分ける多角形の断面(`facet_ring`、角を 2 点にしてスムーズシェードでも面の中ほどを平らに)。肩・腿の付け根の断面も箱寄り(sq 2 → 2.9)、肩の付け根を前後に広い板に |
| 塗り | 地 #D8754C・背 #E48A56(撮影で明るく桃色寄り)・淡い #B08C78 | 地 #CC6E40・上を向く面 #E49C5C(黄み)・陰 #B2623A(脇の下側・腿の裏・尾の下)・淡い #A8856C。額と鼻づらの上・肩と腿の上の面も明るく |
| 光 | 背の稜線・棘・継ぎ目 3 本・目 | 肩の前の線を胸の前まで下ろして正面の輪郭へ回し、尻の後ろの輪郭の線と眉(目頭の上から耳の側へ上がる短い線)を足した。目を 1 割大きく |
| 前脚 | rest で垂直 | 手首から先を 4 cm 前へ(前腕が前下へ傾く)。後足は 2 cm 後ろ。前脚の間を 0.10 → 0.112 m に開いた |
| 尾 | 長さ 0.53 m・最も太い所 0.078 m | 0.45 m・0.092 m |
| 接地 | fall で足が 0.21 m、pounce 2.7 cm、stalk 1.5 cm 地面へ潜る | 足の底の目安の点で遊脚・蹴り出しの足の付け根を持ち上げ(`paw_clear`)、fall は IK で足を残してから横倒し、各フレームで接地を合わせる。全アクション z = 0 |
| 三角形 | 2,541 / 650 | 2,639 / 690(稜線 44 → 36 区間、脚の断面 11 → 10 で光を足した分を返した) |

手直し前の比較画: ![前](wolf-compare-before.png)

## 基準画との差(残り)

手直しの後に残る差(手直し前の記述は「手直しの前と後」の表の「前」の列)。

- **面の立ち方**: 胴の背・脇・腹の面は分かれたが、基準画ほど肩甲骨の板・腿の板の縁は立たない(継ぎ目の光で区切りを補っている)。頭は丸みが残り、側面では基準画より長く見える。
- **鼻づら**: 狐寄りの細さ・黒い玉は解消。基準画の頬の淡い張り出し(頬の飾り毛の広がり)は小さい。
- **塗り**: 面ごとの明暗は頂点色で付けたが、筆の塗り・毛の筆致は無い。淡い色は基準画(灰茶)よりまだ暖かい。
- **発光**: 基準画は輪郭全体を包む滲んだ光。モデルは線の光で、肩の前・尻の後ろ・眉まで。正面では稜線(首の上の棘)が頭の上に細い線として見えるのは前と同じ。
- **姿勢**: 前脚は前へ傾いたが、基準画の側面ほど突き出さない(原点を保つため 4 cm に留めた)。
- **尾**: 下側のぎざぎざの毛は無い。
- **群れ LOD**: 足の塊と飾り毛を省き、脚のロフトの先を平らにしただけ。遠目の輪郭は近 LOD と同じ。
- Three.js での読み込み・再生の確認はまだ(`creatures.ts` は鹿の GLB だけを読む。狼の GLB を読み込む配線は M22-05 の残り)。
  → 追記: 配線は 9243491 で済み(`RIGS`)。今回の手直しはノード名・骨・クリップ名と長さを変えていないので配線はそのまま。Three.js での見え方の確認はまだ。

## 判定(所見)

珊瑚色の体・焦げ茶の脚・淡い喉と胸・シアンに光る背の稜線と棘・光る目・尖った耳・ふさふさの尾・肩が高く頭の低い構えは、5 方向とも基準画と同じ生き物として読める。
面の立ち方と鼻づらの形は基準画に届いていない(狐寄り)。鹿と揃えて、Three.js の塗りを当てた後に詰めるかをユーザーの美観チェックで決める。

## 作り直し(2026-09-24 審査台 t05-wolf の不合格)

ユーザーの判断(原文): 「狐に見える。折り紙のよう。全体的にフォルムがまるっこいのでシャープにしたうえで形状の再現性をあげること。顔まわりはもっと形状も段差も制裁にする。眼および歯を内包する口の鋭さを出す。テクスチャは毛皮の感じが出して野生味をあたえる」。
上の「出力と作り直し方」「実測」「材質」「比較画」の表は作り直しの前の値のまま残す(前の画像は `wolf-*.png`、後は `wolf2-*.png`)。作り直し方のコマンドは同じで、比較画だけ名前の頭と前の撮影を渡す。

```sh
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_wolf.py -- assets/models/observe
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_wolf_render.py -- assets/models/observe/wolf.blend <raw_dir> 720
python3 tools/blender/observe_wolf_sheet.py <raw_dir> docs/design/qa/observe wolf2 <作り直し前の raw_dir>
# 形だけ確かめるとき (アニメ・遠い段・GLB を飛ばす)
WOLF_FAST=1 ~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_wolf.py -- <scratch_dir>
```

### 何を変えたか(`tools/blender/observe_wolf.py` の「灰狼の作り直し」の節、組み立ては `build_lod2`)

| 判断の点 | 前 | 後 |
|---|---|---|
| 狐に見える | 丸い頭蓋から細く尖る鼻づら、細い脚、白っぽい先の太い尾 | 頭は手で置いた断面 (`HEAD2_KEYS`) のロフト: 平らで幅の広い頭頂、張り出した頬骨、額から鼻づらへの段(ストップ)、箱の鼻づら(先の幅 11 cm・深さ 10 cm)。首を太く(横半径 +1.5 cm)、脚を 1 割太く、胸を深く腹を巻き上げ、尾は真下寄りに垂らして先を焦げ茶に(狐の白い先にしない)。耳は付け根の広い角ばった三角で、頭蓋の外寄りに立てる |
| 折り紙のよう | 背の棘・頬と胸の飾り毛は薄い楔の三角 (紙を折ったような板) | 立体の毛の房 (`tuft`: 菱形・五角形の断面の曲がった尖った塊、根元の色から先の色へ)。面に沿って寝かせ、根元を面に沈めて先を重ねる(鱗の並び)。頬の飾り毛 2 列・首の飾り毛 3 列・背の逆立つ毛(先の上の面が光る)・胸・肘・腿の裏・腹・尾(上と横と下)。背の稜線と脇の継ぎ目の光は基準画どおり残した |
| まるっこい → シャープに | スムーズシェードだけ | 頭・下顎・耳・鼻・足は 34〜60° より折れた辺を硬い辺に (`make_part(sharp=…)`)。胴の断面の角の丸め (`facet_ring` の bevel) を 0.2 → 0.08 |
| 顔まわりの形と段差 | 楕円の断面の楔、眉は細い光の線 | 眉の稜が眼窩の上に張り出し(稜と眼窩の差 2.6 cm)、眼窩はくぼむ。口の縁に唇の帯(黒い頂点色)、目の周りの暗い隈と目頭から前下への涙の線。頬骨に沿う光る線を足した |
| 眼の鋭さ | 楕円に近い光る目 (目尻を伸ばす) | 眼窩に沈めた吊り上がったアーモンド (`almond`: 上瞼は平ら、目尻を 1.55 倍に尖らせ、目頭は前下へ約 29°)。暗い縁の中に細い光る虹彩 |
| 歯を内包する口 | 顎の骨はあるが口の中・歯は無い | 下顎を別の部品にし(顎の骨の付け根を口の角の後ろの蝶番に移した)、口の中(口蓋・口の床)を暗い色、歯を 28 本の錐 (`build_teeth2`: 上下・左右それぞれに牙 1・門歯 3・臼歯 3、材質 `wolf_teeth`)。口を閉じた rest でも上の牙の先が下唇の外に見える。stalk は顎を 7° 開けて牙と歯を見せ、run は 10° で息をし、pounce は 26 → 32°(溜めで 6°) |
| 毛皮のテクスチャ・野生味 | 頂点色だけ | 毛皮のテクスチャ `assets/textures/observe/wolf_fur.png`(512²、numpy で生成・乱数の種 7 で毎回同じ絵。外から取った素材は使っていない)を毛の材質 `wolf_body` に掛ける(glTF では baseColorTexture × COLOR_0、JPEG で埋め込む)。根元が暗く先が明るい細長い筆の房 2,000 本を毛並みの向き (+v) に重ね、房の縁を暗く、大きな明暗のむらを薄く。1 枚 0.36 m。UV は部品の軸に沿う円筒の展開 (`uv_faces`、毛並みは頭 → 尾・肩 → 足の向き)。テクスチャで暗くなる分 (平均 0.68) は頂点色を 1/0.68 倍して戻す (`fur_gain`) |
| 脚・足 | 焦げ茶の材質 `wolf_dark`、丸い塊の足 | 焦げ茶は頂点色(肘・膝の少し上から、`wolf_dark` と `wolf_pale` の材質はやめた)。足は掌の塊・指 4 本・暗い爪 |

### 実測(`observe_creature_check.py`、GLB を読み戻した値)

| メッシュ | 三角形 前 → 後 | 材質別(後) |
|---|---|---|
| `wolf`(近 LOD) | 2,639 → **5,995** | body 5,001 / glow 556 / nose 322 / teeth 116 |
| `wolf_lod1`(群れ LOD・影) | 690 → **1,098** | body 907 / glow 113 / nose 78 |
| `wolf_far`(遠い段) | 150 → **176** | body 148 / glow 14 / nose 14(55 島のうち 13 島を残す) |

- 材質 5 → 4(`wolf_body`(テクスチャ + 頂点色)・`wolf_teeth` #EDE4D0・`wolf_nose`・`wolf_glow`)。GLB 401 → 696 KB(テクスチャ込み)。
- 全アクションで一番低い頂点は idle / run / pounce -0.000、walk / stalk -0.001、fall -0.003 m(3 メッシュとも。fall は近 LOD で接地を合わせるので、群れ LOD・遠い段がわずかに下へ出る)。
  足を指と爪のある形にしたので、接地の目安の点 `PAW_PTS` に指の底と爪の先を足した(足さないと walk・stalk で 1.4 cm 潜った)。ループの最初と最後の差 0.0000、root の移動 0。
- glTF-Validator(npm `gltf-validator` 2.0.0-dev.3.10): **errors 0 / warnings 0**、infos 7(テクスチャを持たない材質のプリミティブにも UV が付く UNUSED_OBJECT)、アニメ 6、材質 4。
- 骨・クリップ名・長さ・ノード名(`wolf` / `wolf_lod1` / `wolf_far`)は変えていないので、`RIGS`・VAT・影(群れ LOD)の配線はそのまま。

### 観察画面の計測(`npm run bench:observe`、同じ時刻に古い GLB と新しい GLB を続けて測った `.bench/wolf2-before2.json` → `wolf2-after2.json`)

| 画 | 三角形 (千) | draw call | 動物の三角形 (千、描く / 影) |
|---|---|---|---|
| 集落 | 282.3 → 284.0 | 95 → 100 | 43.9 → 57.4 / 11.9 → 13.5 |
| 船台 | 232.1 → 232.2 | 87 → 88 | 41.0 → 41.1 / 9.8 → 9.8 |
| 群れ | 217.2 → 221.0 | 74 → 76 | 54.1 → 57.5 / 14.0 → 14.4 |
| 林 | 325.4 → 326.1 | 87 → 89 | 19.7 → 23.4 / 3.8 → 4.2 |
| 狼 | 341.8 → 355.2 | 102 → 111 | 46.0 → 61.1 / 10.2 → 11.8 |
| 海岸 | 225.0 → 225.4 | 70 → 71 | 41.4 → 44.8 / 10.6 → 11.0 |

- 三角形は近くの骨入りの 4 頭 × (5,995 − 2,639) で +13.4 千、影と群れ LOD は +0.4 千 / 頭。
- draw call は近くの狼 1 頭につき +1、影の群れ LOD 1 頭につき +1。前は材質 5 つが `bake.ts` で 1 つの焼いた材質にまとまっていたが、テクスチャを持つ `wolf_body` はまとめられず別に描く(観察画面のページで狼のメッシュを数えて確かめた: 近 LOD は `wolf_body` + 焼いた残り 3 材質の 2 つ)。
- fps は同じ GPU で別の作業(ほかの担当の Playwright)が動いていて揺れたので比べない(同じ GLB で 2 回測って 集落 122 / 127、狼 138 / 111)。

### 画像

| 画像 | 中身 |
|---|---|
| `wolf2-compare.png` | 基準画と作り直し後(`wolf-compare.png` と同じ並び) |
| `wolf2-head.png` | 頭の寄り: 基準画の頭(側面・正面・斜め前)/ 作り直し前 / 作り直し後 / 後の stalk(口を少し開く)・pounce(開く) |
| `wolf2-view-{side,front,q34,rear34}.png`・`wolf2-lod.png`・`wolf2-anim.png`・`wolf2-seq-*.png` | `wolf-*.png` と同じ中身の作り直し後 |
| `wolf2-ingame.png` | 観察画面(`observe.html?time=0.12&freeze=1&ship=60&shot=狼`)の前後、近い忍び寄り(stalk、本体を 10x で回して狩りを起こす)、追う途中の口を開いた姿、斜め前 |
| `wolf2-ingame-chase.png` | 追う途中の拡大(口の中の歯・毛皮のテクスチャ)。耳を 1 段大きくする前の版で撮った(耳の他は同じ) |

![比較](wolf2-compare.png)
![頭](wolf2-head.png)
![観察画面](wolf2-ingame.png)

### 自分で見た所見(判断の点ごと)

- **狐に見えるか**: 正面・斜め前の顔は、幅の広い頭頂・張り出した眉と頬・箱の鼻づら・頬と首の飾り毛で狐より狼に寄った。側面は鼻づらがまだ基準画より長く、珊瑚色の体と淡い胸の配色は基準画どおりなので、遠目の色の印象は変わらない。
- **折り紙のようか**: 薄い楔の飾り毛と棘をやめ、毛の房とテクスチャで面が毛皮になった。背の稜線と脇の継ぎ目の光る線は基準画の意匠として残したので、線で区切られた面の印象は残る。
- **まるっこさ**: 頭・顎・耳・足の辺は立った。胴は断面の角を立てたが、テクスチャと房で覆われて輪郭は基準画ほど角ばらない(肩甲骨・腿の板の縁は基準画に届いていない)。
- **顔まわり・眼・口**: 眉の稜と眼窩の段、唇の線、吊り上がった細い目、上の牙は rest でも見え、stalk・pounce で歯の列と暗い口の中が見える。基準画の目は大きく光るが、モデルの目は細く鋭い方へ寄せた(基準画より小さい)。
- **毛皮・野生味**: Blender の撮影では筆の毛並みがはっきり見え、観察画面でも 3〜10 m で毛並みの筋と房の重なりが読める。群れの距離(20 m より先)では平均の色に溶ける。
- 残り: 側面の鼻づらの長さ、耳は基準画より小さい、胴の板の縁、群れ LOD の足(脚のロフトの先の平らな板のまま、前から同じ)。観察画面で頭に 2 m まで寄った画は撮れていない(自由カメラの最小距離 `controls.minDistance = 4` m で戻される)。頭の寄りは Blender の `wolf2-head.png` で見る。
