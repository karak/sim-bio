# 観察画面の月鹿(M22-02 デフォルメ試作)

基準画 `assets/textures/board/creatures/deer.png`(承認済み)を、Switch 世代の 3D ポケモン程度の密度(丸めた形・なめらかな陰影)で起こした月鹿。
作り方は設計 §9 の A(`tools/blender` の部品集)。既存のローポリ鹿(`tools/blender/deer.py`)の意匠(青緑の装甲板・シアンの継ぎ目・三日月の角)を引き継ぎ、形は基準画の画素から測り直した。

## 出力と作り直し方

| もの | 場所 |
|---|---|
| モデル | `assets/models/observe/deer.glb`(+ `deer.blend`) |
| 組み立て | `tools/blender/observe_deer.py` |
| 撮影 | `tools/blender/observe_deer_render.py`(EEVEE、紙色の背景、左上前からの柔らかい光) |
| 比較画 | `tools/blender/observe_deer_sheet.py`(python3 + Pillow) |
| 書き出しの検証 | `tools/blender/observe_deer_check.py`(GLB を Blender に読み戻して数える) |

```sh
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_deer.py -- assets/models/observe
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_deer_render.py -- assets/models/observe/deer.blend <raw_dir>
python3 tools/blender/observe_deer_sheet.py <raw_dir> docs/design/qa/observe
~/.claude/skills/blender/scripts/run_blender.sh tools/blender/observe_deer_check.py -- assets/models/observe/deer.glb
```

## 実測(`observe_deer_check.py`、GLB を読み戻した値)

| メッシュ(ノード名) | 三角形 | 頂点 | 内訳(材質別の三角形) |
|---|---|---|---|
| `deer`(近 LOD、雄) | **3,166** | 2,299 | body 1,770 / glow 700 / plate 332 / hoof 260 / antler_base 104 |
| `deer_lod1`(群れ LOD) | **866** | 773 | body 494 / glow 184 / plate 108 / hoof 60 / antler_base 20 |
| `deer_doe`(雌、角なし、近 LOD と同じ密度) | 2,794 | 2,089 | body 1,770 / glow 408 / plate 332 / hoof 260 / antler_base 24(目の縁) |

- 予算(設計 §8: 近 3,000 / 群れ 900)に対し 近 +6%、群れ -4%。GLB は 618 KB(3 メッシュと 5 アクション込み。graze が 10 s に延びた分増えた)。
- (M22-05 残りの手直し 2026-09-24 の値。手直し前は 近 3,126 / 群れ 872 / 雌 2,754、GLB 525 KB)
- 寸法: 単位 m。き甲(毛の背の線)1.41 m、角の先まで 2.90 m、角の幅 1.44 m、鼻先から尻 2.09 m。蹄の底が z = 0、前後の蹄の中点が原点(前蹄の先 -0.51、後蹄の先 +0.55)。
- 向き: Blender で正面 -Y → glTF で **+Z が正面、Y が上**。
- 3 つのメッシュは同じスキン(骨 28 本)を共有し、どれもシーンのルートに置く(アーマチュアの子にしないので glTF-Validator の NODE_SKINNED_MESH_NON_ROOT が出ない)。
- 1 頂点あたりの影響は最大 3 本、重みの合計はすべて 1。
- glTF-Validator(npm `gltf-validator`): **errors 0 / warnings 0**、アニメ 5、材質 5、スキンあり。

## 骨(28 本)

`root`(動かさない)・`pelvis`・`spine1`・`chest`・`neck1`・`neck2`・`head`・`jaw`・`ear_L/R`・`tail1/2`、
脚 4 本 × 4(前: `fl_upper/fore/cannon/hoof_{L,R}`、後: `hl_thigh/shin/cannon/hoof_{L,R}`)。角・目は `head` に固定。
どの骨もローカル X がワールド X(左右)を向くようにロールを揃えてあり、脚と背骨の曲げはローカル X 回りの回転だけで表せる。

## アニメーション(30 fps、その場。root と骨盤の水平位置は全フレームで 0)

| 名前 | 長さ | ループ | 中身 |
|---|---|---|---|
| `idle` | 4.0 s(120 f) | する | 呼吸(2 回)、首をゆっくり左右、1.0 s に左耳・2.7 s に右耳をはじく、2.0 s に尾を振る |
| `walk` | 1.2 s(36 f) | する | 4 拍の常歩(左後 → 左前 → 右後 → 右前、立脚 64%、歩幅 0.5 m)。首の上下、尾の揺れ |
| `run` | 0.6 s(18 f) | する | 回転ギャロップ(右後 → 左後 → 右前 → 左前、立脚 36%)。背の伸縮、耳を伏せる |
| `graze` | 10.0 s(300 f) | する | 0.3〜2.2 s 頭を下ろし、前足(手首)を曲げて前半身ごと 7 cm 沈め前へ 8° 傾ける。2.4〜4.2 s と 5.6〜7.6 s は頭を下ろしたまま食む(顎を 1.6 回/秒で小さく開け閉め、1.25 s ごとに草をちぎる小さな引き、鼻先を左右へゆっくり)。4.3〜5.2 s に左前足をゆっくり 17 cm 前へ(体重も 3.5 cm 前へ)。7.9〜9.7 s にゆっくり頭を上げて横を見回し、8.0〜8.9 s に左前足を体の下へ戻す。ループの継ぎ目は頭を上げた姿勢 |
| `fall` | 2.0 s(60 f) | しない | 0〜0.6 s 前膝をつく(前足は IK で手首を折る)、0.45〜1.4 s 右側を下に横倒し、1.1〜2 s 首を前へ伸ばして地面に置く。頭は体と一緒に転がさず、世界に対して起こしたまま(角は上)顎を地面に付ける。途中で脚が地面より下へ出るフレームは出た分だけ体を持ち上げる(最大 0.15 m、前後 2 フレームでならす) |

- 脚は 2D の解析 IK(肩・股関節から球節)で、立脚中は蹄が地面に固定される。ループはどれも最初と最後のフレームが同じ。
- 検証(`observe_creature_check.py`、GLB を読み戻して 3 メッシュとも全フレーム): 一番低い頂点 idle -0.000 / walk -0.001 / run -0.001 / graze -0.001 / fall -0.000 m、ループの最初と最後の姿勢の差 0.0000(idle・walk・run・graze)。root の移動 0、骨盤の前後の揺れは graze だけ 0.035 m(一歩で体重を前へ)。
  手直し前の fall は -0.926 m(右の角が地面に刺さる)だった。
- 全 28 骨を毎フレーム焼いてある(クリップを切り替えても前のクリップの姿勢が残らない)。`fall` は Three.js で `LoopOnce` + `clampWhenFinished`。

## 材質

| 名前 | 色(sRGB) | 使い方 |
|---|---|---|
| `deer_body` | 頂点色 `COLOR_0`(地 #CB9C5E、背と脚の下 #B5854F、腹・喉・胸・顎の下 #F0DAAA、鼻づら #E4C288、耳の内側 #EFCFA8)、baseColorFactor 1 | 毛。淡い腹は頂点色で持ち、テクスチャは使わない |
| `deer_plate` | #3A766E | 装甲板。縁と面取りは硬いエッジ、頂はなめらか |
| `deer_hoof` | #1F4B47 | 蹄と鼻 |
| `deer_antler_base` | #24514C | 角の根元、目の縁 |
| `deer_glow` | #8FF5E6、emissive #8FF5E6(係数 1) | 角・目・装甲板の継ぎ目・腹の線・胸の V 字 |

- 全材質 roughness 0.8、metallic 0、specular 0(`KHR_materials_specular`)。陰影は Three.js 側のトゥーンのランプで付ける前提。
- COLOR_0 は全プリミティブに付く(毛以外は白)。GLTFLoader は COLOR_0 があると `vertexColors` を立てるが、白なので毛以外の色は変わらない。

## 比較画

基準画(上段)とモデル(下段、rest、同じ向き)。撮影の発光は強さ 0.45 + グレア(Three.js の bloom の代わり)。

![比較](deer-compare.png)

| 画像 | 中身 |
|---|---|
| `deer-compare.png` | 側面・正面・斜め前・斜め後ろ(基準画 上段 4 体と同じ向き) |
| `deer-view-{side,front,q34,rear34}.png` | モデル単体 512² |
| `deer-lod.png` | 近 LOD / 群れ LOD / 基準画の雌 / モデルの雌 |
| `deer-anim.png` | idle(1.1 s、耳)・walk(9 f)・run(4 f)・graze(2.5 s、食む)・fall(最後) |
| `deer-seq-{walk,run,graze,fall}.png` | 側面の連続(脚の順序、頭の上げ下げ、倒れ方)。graze は 30 f(1 s)ごと、fall は 10 f ごと |
| `deer-plates-eyes.png` | 装甲板と目の手直しの前後(基準画を同じ縮尺で切り出した側面・目、斜め前、正面の顔) |

![LOD と雌](deer-lod.png)
![アニメ](deer-anim.png)

## 作り直しの記録

1. **1 回目**: 胴・首・頭・脚を断面のロフトで組み、装甲板を胴の軸から放射状に投影した。胴が長い筒、首が細い、頭と耳が小さい、角は外へ開くだけ、板は角を丸めすぎて楕円の塊、発光が白飛び。
2. **2 回目**: 基準画の側面と正面を画素で測り直した(き甲 176 px = 1.4 m、0.00795 m/px)。腹を 0.76 m まで下げて胸を深く、首を太く立て、頭を大きく。
   角を基準画どおりに作り直した(深緑の根元は頭頂から外・後ろへ、発光する主幹は外・後ろへ張り出してから上がり、先が前・内へ巻く三日月。前・内へ出て上を向く眉枝と中ほどの枝)。
   装甲板の輪郭を側面図の (y, z) で書き直し、胴・首・脚の付け根をまとめた殻へ投影するようにした(腰の板が腿へ、肩の板が上腕へかかる)。
3. **3 回目**: 板の角を丸めずに辺を割るだけにし(六角形の角を残す)、縁と面取りを硬いエッジ・頂をなめらかにした。板の色を明るく、喉から胸の前まで淡い色を続け、首と胴の継ぎ目の色の段差を消した。肩の上の板をき甲へ移した。
4. **4 回目**: アニメの向きの取り違えを直した(graze で首が背中側へ反っていた。ローカル X の正は「前・下へ倒す」)。fall の頭を起こし気味にロールして角が地面に刺さらないように、run は首を前へ伸ばす。
   角の根元を長く外へ、板の厚さ 7.5 cm。群れ LOD の蹄が地面の下へ 4 cm はみ出していたのを直し、き甲を 1.41 m に下げた。メッシュをアーマチュアの子から外して Validator の警告を 0 にした。

5. **M22-05 残りの手直し(2026-09-24)**: ユーザーの判断(試作 1「装甲板や瞳の形状などはもう少し忠実度をあげたい。シルエットや各部位のフォルムは現状ママ」、2026-09-24「首だけおもちゃのように上下を繰り返すのはおかしい…」)に沿って直した。
   - 装甲板: 輪郭を基準画の側面から測り直した(y = (252 − px) × 0.00795、z = (386 − py) × 0.00795)。肩の板は前上の長い斜めの辺と前へ尖った角を持つ六角、き甲の板は背の上に立つ前上がりの平行四辺形(厚さ 9 cm)、腰の板は前へ尖った六角。
     輪郭を真横から胴へ投影する(従来は上下の点ほど視線を傾けたので、側面から見ると輪郭が崩れ、き甲の板は背の線を越えて反対側へ回り込んでいた)。面取りは辺から 3 cm の一定幅、頂は横の稜線で上下 2 枚の平らな面に割り(上が明るく下が暗い切り子)、全面フラットシェード。
   - 光る継ぎ目: 板の縁から 2.2 cm 離して胴に沿わせる帯にした(角で折れる、幅 2.2 cm)。腰の板の前 2 辺(背の線から)、き甲の板の前の辺 → 肩の板の前上の斜めの辺 → 胸の V 字、き甲と肩の板のあいだ。腹の線は基準画どおり腹の中ほどで下へ折れる V。従来の板の縁の帯はギザギザに見えていた。
   - 目: 楕円からアーモンドへ。暗い縁は目尻を後ろ上へ尖らせ、目頭は丸く、目頭が 20° ほど下がる。光る瞳は丸みを残して前へ寄せる。
   - graze を 10 s に作り直し、fall の頭の向きと接地を直した(上の表)。

## 基準画との差(残り)

- **装甲板**: 基準画は面が立った宝石のような六角の板で、明るい面取りと暗い面がはっきり分かれる。モデルの板は輪郭と配置は合っているが、厚い平板に近く、面の明暗が弱い。肩の上の板が小さい。
  (手直し後: 輪郭・稜線・面取りは寄ったが、基準画の縁の柔らかいハイライトは無い。き甲の板は基準画よりまだ小さく低い。腰の板の下の角は腿の丸みに沿って少し歪む)
- **塗り**: 基準画の筆の塗り(暖色のハイライト・寒色の影・毛並みの筆致)は無い。Blender の撮影は素の拡散光。トゥーンのランプ・縁の光・色調補正は Three.js 側の仕事(M22-02 の残り)。
- **胴の比率**: 胴が基準画より 1 割ほど長く、背の線が平ら、尻の丸みが弱い。
- **角**: 形と大きさは合っている。基準画の角は面の立った太い角で、モデルは六角断面のなめらかな管。撮影では発光が基準画より白寄り。
- **耳**: 雌の耳は基準画ほど大きく立っていない。
- **動き**: graze は首の付け根を 115° 曲げるので、寄ると付け根がつぶれる(手直し後は 86° + 中ほど 40°。前半身を沈めた分だけ減ったが、寄ると付け根はまだ詰まる)。倒れる途中(0.8〜1.2 s)は脚で体を支えて少し浮く。fall の横たわった脚は折れて重なり気味。重みは骨までの距離で配っているので、run で肩の板が上腕と一緒にわずかに曲がる。
- **子鹿**: 作っていない(斑点は頂点色では粗く、装甲なし・別の毛色の材質が要る。必要なら 512² のテクスチャで別に起こす)。
- **民の雄の紐**: 作っていない(角の根元に結ぶ紐は民の個体の付け足しとして別ノードで足せる)。
- Three.js で読み込んで表示・アニメを再生する確認はまだ(この文書の検証は Blender の読み戻しと glTF-Validator まで)。

## 判定(試作としての所見)

造形の要点(黄褐色の体と淡い腹・喉、肩と腰の青緑の板と前縁の光る継ぎ目、腹の線と胸の V 字、深緑の根元から伸びる三日月の光る角、光る目、深緑の蹄、立った小さな尾)は、
4 方向とも基準画と同じ生き物として読める。密度は「角ばりを丸めたなめらかな形」に届いているが、基準画の絵としての質(板の面の立ち方、筆の塗り)には届いていない。
塗りを Three.js のシェーダで寄せたあとに、板の面の作り込みと胴の比率をもう一段詰めるか、AI の 3D 生成(設計 §9 の B)と比べるかをユーザーの美観チェックで決める。

## 手直し 2(2026-09-24 審査台 t05-deer の合格、注文あり)

ユーザーの判断(原文): 「装甲板や角の淵のハイライト。眼の形状の細かさは三角形を増やしてでもより忠実に」。
上の表と画像は手直し 2 の前の値のまま残す(前の画像は `deer-*.png`、後は `deer2-*.png`)。組み立ては `tools/blender/observe_deer.py`、撮影・検証は種ごとの一般化版 `observe_creature_{render,check}.py`。
面は平らな塗り分けのまま(テクスチャは使わない)。目は神の使いの穏やかな目にしたいので、目尻を吊り上げない。

### 何を変えたか(`observe_deer.py` の「月鹿の手直しで追加」)

| 判断の点 | 前 | 後 |
|---|---|---|
| 装甲板の縁のハイライト | 面取り (縁の壁の上から辺の 3 cm 内側まで) も板と同じ #3A766E | 面取りのうち、側面図で外向きの法線が下を向きすぎない辺 (`PLATE_HI_MIN_NZ`、下向きの辺は陰のまま) を明るい青緑 `deer_plate_hi` #62A396 に。基準画の板の縁取りのような明るい縁が上・前・後ろの辺に出る。近 LOD と雌 |
| 角の縁のハイライト | 六角断面のなめらかな光る管(一色) | 断面の上側の 2 つの角を ±7° の 2 点に割って細い稜の面を立て、その面を白に近い光 `deer_glow_hi` #E2FFFA(発光の強さは `deer_glow` と同じ)に(`tube(..., ridge=)`、`ANTLER_RIDGE`)。主幹と枝の上の縁に明るい線が通る。近 LOD だけ |
| 眼の形 | 縁 12・瞳 12 頂点。暗い縁は目尻だけ尖り目頭は丸いしずく、瞳は小さく前寄り | 基準画の目(側面・斜め前・正面、concept の寄り)に合わせて両端の尖った細長いレンズ(`EYE_HI`)。目頭は前下へ細く尾を引いて尖り(`pf` 1.6)、目尻も後ろへ尖る(`pb` 1.3)。目尻の持ち上げは 0.7 → 0.25 にして吊り目にしない。暗い縁は上下とも細く、両端で太る。光る瞳は縁の中をほぼ満たす卵形(長さ 6.9 cm・高さ 4.0 cm、基準画の側面の 6.8 × 4.1 cm)で中ほどに置く。頂点は縁 28・瞳 24 |

- 材質 5 → 7(`deer_plate_hi`・`deer_glow_hi` を足した)。観察画面では `bake.ts` が絵を持たない材質を 1 つの焼いた材質にまとめる(色は頂点色、発光は頂点の属性)ので draw call は増えない(計測で確かめた)。
- 撮影 (`observe_creature_render.py`) は `<種>_glow_hi` があればその発光も 0.45 で撮る(1 行の足し)。

### 実測(`observe_creature_check.py`、GLB を読み戻した値)

| メッシュ | 三角形 前 → 後 | 材質別(後) |
|---|---|---|
| `deer`(近 LOD、雄) | 3,166 → **3,350** | body 1,770 / glow 732 / glow_hi 92 / plate 244 / plate_hi 88 / hoof 260 / antler_base 164 |
| `deer_doe`(雌) | 2,794 → **2,850** | body 1,770 / glow 432 / plate 244 / plate_hi 88 / hoof 260 / antler_base 56 |
| `deer_lod1`(群れ LOD・影) | 866 → **866** | 変えていない |
| `deer_far`(遠い段) | 210 → **210** | 変えていない(群れ LOD から削るので同じ) |

- 増えたのは角の稜 (+128) と目 (+56)。予算(近 3,000)に対し +12%(目は「三角形を増やしてでも」の注文)。GLB 636 → 656 KB。骨・クリップ・ノード名は同じ。
- 全アクションで一番低い頂点は idle / fall -0.000、walk / run / graze -0.001 m(前と同じ)、ループの最初と最後の差 0.0000、root の移動 0。
- glTF-Validator: **errors 0 / warnings 0 / infos 0 / hints 0**、アニメ 5、材質 7。同じスクリプトを 2 回回して GLB がバイト単位で同じ。

### 画像

| 画像 | 中身 |
|---|---|
| `deer2-compare.png` | 基準画 / 手直し 2 の前 / 後(側面・正面・斜め前・斜め後ろ) |
| `deer2-head.png` | 基準画の目(側面・斜め前・正面・concept)/ 前 / 後(目の側面・斜め前・正面・寄り) |
| `deer2-plates.png` | 基準画の板・concept の板・基準画の角 / 前 / 後(板の側面・肩の板の寄り・斜め前・角の側面・正面) |
| `deer2-ingame.png` | 観察画面(`shot=群れ`、自由カメラで近くの雄へ寄り、画角 22°・16°)の斜め前・板の寄り・食む群れと既定のカメラ |

![比較](deer2-compare.png)
![目](deer2-head.png)
![板と角](deer2-plates.png)
![観察画面](deer2-ingame.png)

### 自分で見た所見

- **板の縁**: 上・前・後ろの面取りが明るい縁になり、基準画の縁取りの明るさに近づいた。観察画面でも寄ると縁が読め、群れの距離(群れ LOD)では前と同じ(面取りを持たない)。基準画の板の面の中の明暗(筆の塗り)は無い。
- **角の縁**: 主幹・枝の上の縁に白い線が通り、面の立った角に見える。観察画面では bloom で線が柔らかく溶ける。基準画は角の内側の面全体が明るく外側が暗いが、モデルは稜の線だけ(面の塗り分けは足していない)。
- **目**: 両端の尖ったレンズと、縁の中を満たす瞳は基準画の側面・斜め前に近い。目尻を上げないので穏やかに見える。正面から見ると目は頭の横に付いた細い形で、基準画の正面(やや前を向いた目)より細い(頭の形・目の向きは変えていない)。
- 残り: 群れ LOD(と遠い段)の角・板・目は手直し 2 の前と同じ(雌は近 LOD と同じ目・板の縁を持つ)。基準画の目の周りの明るいまぶたの面(concept の切り子)は作っていない。

## 手直し 3(2026-09-24 23:54 手元の審査台 d2-deer、判定なし)

ユーザーのメモ(原文): 「つののハイライトの再現率を上げて。正面がで👀がみえるように鼻筋を通して頬や眼窩の部分も設ける。」
上の表と画像は手直し 3 の前の値のまま残す(前の画像は `deer2-*.png`、後は `deer3-*.png`)。組み立ては `tools/blender/observe_deer.py`、撮影・検証は `observe_creature_{render,check}.py`。
見比べた基準画は `creatures/deer.png` の角(側面・正面)と頭の寄り(正面・斜め前・側面)、元の参照 `concept/deer-angular.png`。面は平らな塗り分けのまま(テクスチャは使わない)。

### 何を変えたか(`observe_deer.py` の「月鹿の手直し 3 で追加」)

| 判断の点 | 前(手直し 2) | 後 |
|---|---|---|
| 角のハイライト | 六角断面の一色の光る管、上側の 2 つの角に細い白い稜 (`deer_glow_hi`) | 基準画の角は曲がりの内側(三日月の内)を向く面が明るく、その両の縁が白く光り、外側の面は暗い青緑。近 LOD の角と枝は断面の 1 つの面を曲がりの内へ向けて組み(`tube_lit`、曲がりの向きは `bend_dirs` で折れ線の曲率から)、内の面を `deer_glow_in` #A8F5EC、その両の縁の細い帯 (±8°) を `deer_glow_hi`、内寄りの横の 2 面を `deer_glow`、外の 3 面を `deer_glow_lo` #58BAB6 に。断面の頂点は 8 のまま(三角形は同じ) |
| 鼻筋 | 超楕円の断面で、鼻筋の面は無い | 近 LOD(雄・雌)の頭を角を持つ断面 (`head_corners_face`) で組む: 頭頂・鼻筋の縁(鼻先へ細く)・眼窩の上の縁・頬の張り・顎の縁。鼻筋の上を向く細い面は明るい色(腹の色へ寄せる)で額から鼻先へ通る。周 12 → 14 |
| 眼窩・頬 | 目は頭の真横の面に載り、正面からは細い縦の線 | 眼窩の上の縁を目の前(鼻先の側)で 3 割内へ寄せ(`HEAD_ORBIT_IN`)、目の載る面を前へ向ける。目はその面へ前寄りの向き (`EYE_FACE_DIR`、前へ 36°) から載せ、長軸の前下がりを 0.25 → 0.42、大きさ 1.12 倍。頬の張りの下は陰(`fur_back`)。鼻筋・眼窩の縁・頬の張りの角は顔(y < -0.66)で硬いエッジ |

- 材質 7 → 9(`deer_glow_in`・`deer_glow_lo`)。観察画面では絵を持たない材質は焼いて 1 つにまとまる(色は頂点色、発光は頂点の属性)ので、draw call は増えない(計測で確かめた、issues/M22-05 の作業ログ)。
- 撮影 (`observe_creature_render.py`) は `<種>_glow_in` / `<種>_glow_lo` があればその発光も 0.45 で撮る(足しただけ)。

### 実測(`observe_creature_check.py`、GLB を読み戻した値)

| メッシュ | 三角形 前 → 後 | 材質別(後) |
|---|---|---|
| `deer`(近 LOD、雄) | 3,350 → **3,390** | body 1,808 / glow 548 / glow_hi 92 / glow_in 46 / glow_lo 138 / plate 244 / plate_hi 88 / hoof 262 / antler_base 164 |
| `deer_doe`(雌) | 2,850 → **2,890** | body 1,808 / glow 432 / plate 244 / plate_hi 88 / hoof 262 / antler_base 56 |
| `deer_lod1`(群れ LOD・影) | 866 → **866** | 変えていない |
| `deer_far`(遠い段) | 210 → **210** | 変えていない |

- 増えたのは頭の周 12 → 14 の分(+40)。角の光る面は 732 + 92 = 824 のまま 4 つの材質に分けただけ。予算(近 3,000)に対し +13%。GLB 656 → 655 KB。骨・クリップ・ノード名は同じ。
- 全アクションで一番低い頂点は idle / fall -0.000、walk / run / graze -0.001 m(前と同じ)、ループの最初と最後の差 0.0000、root の移動 0。
- glTF-Validator 2.0.0-dev.3.10: **errors 0 / warnings 0 / infos 0 / hints 0**、アニメ 5、材質 9。同じスクリプトを 2 回回して GLB がバイト単位で同じ。

### 画像

| 画像 | 中身 |
|---|---|
| `deer3-compare.png` | 基準画 / 手直し 3 の前 / 後(側面・正面・斜め前・斜め後ろ) |
| `deer3-head.png` | 基準画の頭(正面・斜め前・側面)と concept の目 / 前 / 後(頭の正面・目の正面の寄り・斜め 60°・斜め前・側面・雌の正面) |
| `deer3-antler.png` | 基準画の角(側面・正面)と concept の角 / 前 / 後(角の側面・正面・斜め前) |
| `deer3-ingame.png` | 観察画面(`observe.html?time=0.12&freeze=1&ship=60&auto=0`、「群れ」の画から近くの雄の頭の前にカメラを置く)の 3 m・6 m・10 m の正面と斜め(画角 42°)。右は画面の中央の画素を最近傍で拡大した寄り |

![比較](deer3-compare.png)
![頭](deer3-head.png)
![角](deer3-antler.png)
![観察画面](deer3-ingame.png)

### 自分で見た所見

- **角**: 側面・正面・斜め前とも、曲がりの内の明るい面とその両の白い縁、外の暗い青緑の面に分かれ、基準画の角の明暗の出方に近づいた。眉枝・中の枝も同じ塗り分け。観察画面では bloom で内の面と縁が一つの明るい帯に溶けるが、外の暗い面との差は 10 m でも読める。基準画の角は面の数がもっと多い切り子で、枝も長い(形は変えていない)。
- **鼻筋・眼窩・頬**: 正面で額から鼻先へ明るい鼻筋が通り、その両脇の眼窩の面に目が前を向いて収まる。目は正面から両方とも見え、目頭が鼻へ向かって下がる。頬の張りの下の陰で頬が分かれる。観察画面の 3 m・6 m の正面で目が両方読め、10 m でも光る 2 点として見える。側面の輪郭と目の位置は前とほぼ同じ。
- 残り: 群れ LOD(と遠い段)の頭・目・角は手直し 2 の前と同じ。鼻は丸い暗い面のまま(基準画は逆三角のハート形)、口の線は無い。

## 手直し 4(2026-09-25 08:07 手元の審査台 d3-deer、63819d2、判定なし)

ユーザーのメモ(原文): 「顔が全体的に尖りすぎている。シルエット自体は間違っていない。が、頬や眼窩はもっと正面に対して平坦になる。デザイン画のように窪み領域を入れたほうがよい。また、穏やかさを称えた口元を鼻とセットで。」
上の表と画像は手直し 4 の前の値のまま残す(前の画像は `deer3-*.png`、後は `deer4-*.png`)。組み立ては `tools/blender/observe_deer.py`、検証は `observe_creature_check.py`。
見比べた基準画は `creatures/deer.png` の頭の寄り(正面・斜め前・側面)、元の参照 `concept/deer-angular.png` の頭(目の載る一段下がった面・頬の線・口)。
横から見た輪郭(各断面の上下の高さ)は変えていない。角(手直し 3 の内の面と縁の光)も変えていない。

### 何を変えたか(`observe_deer.py` の「月鹿の手直し 4 で追加」、近 LOD の雄・雌。群れ LOD と遠い段は変えていない)

| 判断の点 | 前(手直し 3) | 後 |
|---|---|---|
| 顔の尖り | 頭の幅が目から鼻先へ一様に細る楔(正面の輪郭が V)。鼻先は 1 点に収束 | 頭の半幅を別の表(`HEAD4_RX`)で持ち、目の高さは広く(0.166 m)、目の前で急に絞り、鼻づらは平行に近い幅で先まで(鼻先の手前 0.092 m、前は 0.07)。正面の輪郭は U。鼻先は縮めた断面(`HEAD4_TIP` 0.62)を平らな面で閉じる |
| 頬・眼窩を正面に対して平坦に | 眼窩の面は頭の横(前へ約 20°) | 目の前で幅を急に絞るので、眼窩と頬の面が前・外を向く。眉の稜は外(半幅の 0.94)へ出して額を広く、顎の縁は内へ寄せて頬の下に棚を作らない |
| 窪みの領域 | 無い | 断面に「眉の稜 → 窪みの底(上・下の 2 角)→ 頬の線」を入れ、目のまわり(y -0.73〜-0.99)だけ底を 6〜20 mm 内へ下げる(前ほど深い)。底は陰の色(`socket` #A5773F)、眉の稜と頬の線は明るく(腹の色へ)、角は硬いエッジ。頬の線は目の下の前から後ろ下へ下がる(concept)。周 14 → 16、断面 10 → 12 |
| 目 | 眼窩の面へ前寄りの向きから載せ、頂点を面へ吸い付ける | 窪みの底の中ほど(`EYE4_AT`)へ載せ、縁と瞳は平らな円盤のまま、縁のどの頂点も面の下へ潜らない高さへ目ごと持ち上げる(底の折れ目や段の壁が瞳を突き抜けない)。形・大きさは手直し 3 と同じ |
| 鼻 | 鼻先の丸い暗い面(鼻先の 1 点へ収束する錐) | 正面から見て逆三角(上の辺は緩い山、角は丸く、下は尖る。幅 12 cm)を前上から頭の面へ投影した暗い面(`build_nose4`、deer_hoof) |
| 口 | 無い | 鼻の下の角から短い人中を下ろし、左右へ鼻づらの脇を後ろへ回って口角で少し上がる線(`build_mouth4`、幅 5.5〜3 mm、deer_hoof)。口角を上げて穏やかな口元に。重みは下の面と同じ(`head_weights`)で、食むときの顎と一緒に動く |

- 新しい材質は無い(鼻と口は deer_hoof、窪みの塗りは頂点色)。観察画面では焼いて 1 体 1 draw call のまま。
- 撮影・検証のスクリプトは変えていない。

### 実測(`observe_creature_check.py`、GLB を読み戻した値)

| メッシュ | 三角形 前 → 後 | 材質別(後) |
|---|---|---|
| `deer`(近 LOD、雄) | 3,390 → **3,719** | body 1,956 / glow 548 / glow_hi 92 / glow_in 46 / glow_lo 138 / plate 244 / plate_hi 88 / hoof 443 / antler_base 164 |
| `deer_doe`(雌) | 2,890 → **3,219** | body 1,956 / glow 432 / plate 244 / plate_hi 88 / hoof 443 / antler_base 56 |
| `deer_lod1`(群れ LOD・影) | 866 → **866** | 変えていない |
| `deer_far`(遠い段) | 210 → **210** | 変えていない |

- 増えたのは頭の断面 (+148、周 14 → 16・断面 10 → 12・鼻先を閉じる面) と鼻・口 (hoof +181、鼻 100・人中と口の線 90、前の暗い鼻先の分を引く)。予算(近 3,000)に対し +24%。GLB 655 → 682 KB。骨・クリップ・ノード名・材質 (9) は同じ。
- 寸法: 鼻先 y -1.177 → -1.181 m(鼻の面の厚み 5 mm)、ほかの bbox・き甲の高さは同じ。側面の輪郭は `deer4-mouth.png` の右下(後の絵に前の輪郭を赤で重ねた)で重なる。
- 全アクションで一番低い頂点は idle / fall -0.000、walk / run / graze -0.001 m(前と同じ)、ループの最初と最後の差 0.0000、root の移動 0。
- glTF-Validator 2.0.0-dev.3.10: **errors 0 / warnings 0 / infos 0 / hints 0**、アニメ 5、材質 9。同じスクリプトを 2 回回して GLB がバイト単位で同じ。
- 観察画面(`npm run bench:observe`、前の GLB と差し替えて同じ台で): 群れ 226.8 → 229.4 千三角形、draw call は 6 画とも同じ(集落 97 / 船台 95 / 群れ 80 / 林 91 / 狼 103 / 海岸 77)。fps は参考(ほかの担当と GPU を分けていて 207〜425 で揺れる)。

### 画像

| 画像 | 中身 |
|---|---|
| `deer4-compare.png` | 基準画 / 手直し 4 の前(3 回目 63819d2)/ 後(側面・正面・斜め前・斜め後ろ) |
| `deer4-head.png` | 基準画の頭(正面・斜め前・側面)と concept の頭 / 前 / 後(頭の正面・目の正面の寄り・斜め 60°・斜め前・側面・雌の正面・雌の斜め前) |
| `deer4-mouth.png` | 基準画の鼻と口(正面・斜め前)、concept の口と窪み / 前 / 後(鼻と口の正面・斜め、頭の上から、側面に前の輪郭を赤で重ねたもの) |
| `deer4-ingame.png` | 観察画面(`observe.html?time=0.12&freeze=1&ship=60&auto=0`、「群れ」の画から近 LOD の雄の頭の前にカメラを置く、画角 42°)の 3 m・6 m・10 m の正面と斜め 40°。左から前・後の全体、前・後の画面の中央の画素を最近傍で拡大した寄り |

![比較](deer4-compare.png)
![頭](deer4-head.png)
![鼻と口](deer4-mouth.png)
![観察画面](deer4-ingame.png)

### 自分で見た所見

- **尖り**: 正面の輪郭が V から U になり、鼻先は尖らず平らに閉じる。斜め前でも鼻づらが先まで太さを保ち、錐の先のようには見えない。側面の輪郭は前と重なる。
- **頬・眼窩**: 正面で額・眼窩・頬が前を向く広い面に並び、両目が前から見える。目の前で幅を絞った分、正面から見て鼻づらの脇に頬の面が立つ。
- **窪み**: 目のまわりが一段下がった陰の面になり、上の縁(眉の稜)と下の縁(目の下から後ろ下へ下がる頬の線)が明るい線で読める。窪みは目の前後で閉じる。観察画面では 3 m・6 m で目のまわりの陰として読め、10 m ではほぼ溶ける。concept のような菱形の切り子(面の数の多い段)ではない。
- **鼻と口**: 正面で逆三角の鼻・人中・口の線が「Y と横線」に並び、基準画の正面に近い。横と斜めでは口の線が鼻づらの脇を後ろへ回り、口角で少し上がる(穏やかに見える)。観察画面の 3 m・6 m で鼻の三角と口の線が読め、10 m では鼻の三角だけ残る。
- 残り: 群れ LOD(と遠い段)の頭は手直し 2 の前と同じ丸い鼻先のまま(カメラに近い 12 頭だけが近 LOD)。基準画の正面の鼻の上の縁の丸み(ハート形)は逆三角に寄せただけ。
