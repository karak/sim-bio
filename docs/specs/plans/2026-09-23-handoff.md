# 引き継ぎ: M10R 完了 → 残課題の整理とロードマップの組み直し(2026-09-23 18:20)

新しいセッションはこの文書を最初に読む。次の一手は **§6 の判断待ち** に答えをもらい、承認されたらチケットの改廃を実施すること。

## 1. いまの状態

- ブランチ `main` == `origin/main` == `e260202`(M10R-07 のマージ)。作業ツリーは `docs/specs/plans/2026-09-23-roadmap.md` の追加のみ(この引き継ぎと一緒にコミット済みなら clean)。
- 検証は e260202 時点で全部 green: 単体 517、E2E 21、通し 47(5 分割・約 55 分)。
- マイルストーン M1〜M10、M10R、M20 は完了。チケットは 78 done / 41 todo。
- 遊べる石板は 11 本(沈む欠片・星が落ちる夜・火の山の目覚め・豊かさの罠・生気の飢饉・塔の重さ・祈りに応えるな・霊脈枯れ簡易版・迎撃の塔・空の舟 + 試し読み 5 本)。

## 2. 直前にやったこと(M10R-08 と M10R-07)

どちらもユーザー指定の手順「仮説 → 部品(障害の大きさ・ジレンマ)→ 縮約モデルで成立確認 → 本体計測 → 実装、校正は最後」で作り直した。
数値上成立しないときは係数ではなく **舞台装置・生物の特性(キーアイテム)** で仕組みを足す。

- **M10R-08 空の舟**(`40d6240`): 鐘樹を枯れにくく(growth 0.0025 / mortality 0.0005 / **diffusion 0**)、伐採を一定量(`SHIP_CUT_PER_YEAR = 6`、120 で 20 年)、
  0 年目に「民の林」を予定コマンドで置く(cell 2787・環 4・0.6)、薪の蓄え 0。想定解は UI と同じ環 1・0.5 で再現でき、手動で 40 年目に escaped。
- **M10R-07 祈りに応えるな**(`e260202`): 若い信仰の記憶(`FAITH_YOUNG_STAGE = 3`、段階 ≤ 3 では取り下げでも上限 −0.1、祈りの無い年の回復なし)、
  集落を 2063 に、狼の波を北の谷 1366 に環 3・1.0 で 6 年目から 8 年ごと、予定コマンドに `text`(警告 kind 'event' + 年表)。
  谷への疫病 環 4 で 3 か月(90 tick)の先回りができる。手動で alive。
- 詳細は LD `docs/design/2026-09-22-level-design-faith-economy.md` §8.9・§8.10・§9.4・§9.5、仕様書 §4.30・§4.31、
  プレイテスト `docs/specs/plans/2026-09-23-m10r-0{7,8}-playtest.md`。

## 3. 今回の成果物(未承認の提案)

`docs/specs/plans/2026-09-23-roadmap.md` — 残課題の棚卸し・優先順位・組み直したロードマップ。要点:

- **チケットになっていない負債 D1〜D7**: 通し台本の UI パリティ監査(迎撃の儀式台本が草を環 3 で放っている。tests/slow 407・429 行)、
  舟の行に徴収半径内の材、拒否理由の表示、三度目のあと迎撃の行を畳む、狼の波で狼レイヤーを促す、迎撃の後半 70 年の緊張、通し実行の時間管理。
- **提案する順**: 1 M21 仕上げ(新設、D1〜D6 + M19-03) → 2 M12 気候 → 3 M16 疫病 → 4 M11 星砂 → 5 M14 持ち込み + 歌鳥 →
  6 M13 霊脈 → 7 M15 地形 → 8 M17 生き物 → 9 M18 予言。M19-01/02 配信は随時。
- **承認後の改廃(§5)**: M21 新設、M11〜M18 に `-00 レベルデザイン` チケットを足して校正の depends_on に入れる、M17-04 歌鳥を M14 へ移す、
  M17-02 夢喰いは「種にするか」を LD で再判断、issues/README に「判定行列は UI と同じ手(環 1・0.5)で書く」を追記、**番号は付け替えない**。

## 4. 再スタートの手順

1. `docs/specs/plans/2026-09-23-roadmap.md` を読む(この引き継ぎより詳しい)。
2. §6 の判断待ち(下の 4 点)をユーザーに確認する。
3. 「go」が出たら roadmap §5 の改廃を実施し、`tools/issue-status.sh` の規約に従って issues/ を更新、`issues/README.md` に順序と規約の追記を入れる。
   docs 一本のコミットで良い(実装は含まない)。
4. その後 M21-01(UI パリティ監査)から着手する。最初の作業は tests/slow の草の放流を環 3 → 環 1 に直し、迎撃の塔の想定解が手で勝てるか通し実行で再計測すること。

## 5. 守るべき約束(セッションをまたいで有効)

- main から `feat/<milestone>` を切り、チケットごとにコミット、終わったら `--no-ff` で main に戻して push(**push は main のみ**)。
  `GIT_TERMINAL_PROMPT=0 git -c 'credential.https://github.com.helper=' -c 'credential.https://github.com.helper=!/opt/homebrew/bin/gh auth git-credential' push origin main`
- 受入基準ごとにテスト名 + commit SHA を evidence に入れる(`tools/issue-status.sh <id> <status> [evidence...]`)。
- 通し実行は `SLOW=1` + `caffeinate -dimsu` で `-t` 分割。600 秒を超えるものは背景 + done マーカー + until ループで待つ。**並列 vitest は不可**。
  使い捨ての計測テストは scratchpad に置き、走らせるときだけ tests/slow へコピーして、コミット前に消す。
- 手動プレイは `npm run build && npx vite preview --port 5199` + Chrome DevTools。セル→画面の対応は標高で描画がずれるので実測で作る。
  キャンバスを押す前に必ずチップを押す。石板の祈りは `hidden` で読む。100 倍速で 1 年 ≈ 2.5 秒。
- 通し実行の台本は **UI と同じ放流の大きさ(環 1・0.5・力 4)** で書く。手で勝てない想定解は成立と数えない。
- 既存コメントは削除しない(ユーザーが当該コメントを名指しで指示した場合のみ)。`.env`・個人メール・home の絶対パスを文書やコミットに入れない。
- 仕様に従うだけの実装は sonnet のサブエージェントに委譲してよい(worktree で並列可)。worktree のエージェントは古い HEAD から始まることがあるので `git merge --ff-only <sha>` を指示する。
- identity は `karak <156341+karak@users.noreply.github.com>`。報告は簡潔に日本語で。

## 6. 判断待ち(これを聞くところから再開する)

1. M21(仕上げ)を M11 以降より先に挟んでよいか。
2. M12 気候 → M16 疫病 → M11 星砂 の順でよいか(M11 を先にする案もある。祈り「星の砂を」が今も応えられないままなので)。
3. M19-02 Cloudflare 配信(遊べる URL)をいつ入れるか。
4. 歌鳥を M17 から M14 へ前倒しすること、#12 氷期の到来を M12 に入れるか落とすか。

## 7. 参照

- ロードマップ: `docs/specs/plans/2026-09-23-roadmap.md`
- シナリオ 30 本と実装後の見直し: `docs/design/2026-09-19-scenarios-and-world.md` §3・§6
- 信仰の経済の LD: `docs/design/2026-09-22-level-design-faith-economy.md`(§7 捨てた案・保留、§8 実装で足した仕組み、§9 承認事項)
- チケットの規約: `issues/README.md`
- 公開リポジトリ: karak/sim-bio
