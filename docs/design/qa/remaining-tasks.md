# 3D モデル制作 残タスク一覧 (rabbit / deer / wolf 横断)

2026-09-20 11:30 時点。ブランチ `worktree-concept-art-variants` (最終 `7c67d82`、分岐点 `b09d573` = feat/m4 途中)。
各モデルは中間成果物として利用可能。個体ごとの詳細は `<creature>-remaining-issues.md`、判定は `<creature>-acceptance-criteria.md`、
手順は `verification-workflow.md` を参照。

## 進捗サマリー

- **P1 (受入 FAIL の解消) は完了**。3 個体とも受入基準に FAIL 項目はなく、残りは PARTIAL (シルエット IoU と glow) のみ。
- 評価系は 3 個体共通で整備済み: 個体別分類 (`creature_parts.py`)、平行投影モード (`proj=ortho`)、行・列ごとの区間比較 (`classrows.txt`)、
  色補正ループ (`tune_colors.py`)。生成側の共通部品 (`lowpoly_kit.py`) に鰭 (`fin`) と多角形に沿った面の切断 (`cut_along`) を追加。
- 未着手は P2 の細部 (脚の 2 節化、耳の折れ、角の太さ、目・口) と P3 の運用 (three.js 照明での再検証、手動受入、push / マージ)。

### セッション 2 (2026-09-20) のコミット

| SHA | 内容 |
|---|---|
| `e7fa5ea` | wolf: 頭・首・比率・尾を作り直し、縁線を鰭に。評価に平行投影モード (T2 / T3 / T5 / T12) |
| `25e4b9c` | deer: 角を参照から 3D 復元、パネル境界を直線化、脚・胴・頭を修正 (T1 / T4 / T12b) |
| `7c67d82` | compare_ref.py が `classrows.txt` を出力 (T15) |

## 到達状況

| 個体 | 評価 | 三角形 | シルエット IoU (目標 0.80) | 主要パーツ マスク IoU | 色 ΔE76 < 1 | 受入 FAIL | PARTIAL |
|---|---|---|---|---|---|---|---|
| rabbit | 透視 az 45° | 1863 | **0.81** | fur 0.60 / 耳内側 0.68 / glow 0.33 / dark 0.06 | 達成 (4 パーツ) | なし | B5 glow 0.33、B6 dark 0.06 (陰由来)、B7 耳の面積比、B8 dark 重心 |
| deer | 平行投影 az 135° | 2805 | 0.73 (初版 0.49) | fur 0.72 / dark 0.52 / glow 0.39 | 達成 (3 パーツ) | なし | B2 0.73、B6 glow 0.39、B8 glow 重心 0.04 |
| wolf | 平行投影 az 135° | 2182 | 0.77 (初版 0.58) | fur 0.59 / dark 0.14 / glow 0.33 | 達成 (3 パーツ) | なし | B2 0.77、B4 fur 0.59、B8 glow 重心 0.06 |

- wolf 2 回目: 透視 0.58 → 平行投影 0.63 → 形状修正後 0.77。縦横比 1.53 → 1.73 (参照 1.75)。
- deer 2 回目: 透視 0.49 → 平行投影 0.52 → 角・パネル・脚・胴・頭の修正後 0.73。dark 0.31 → 0.52、glow 0.21 → 0.39。
- rabbit は 1 回目 (`0aae098`) のまま。平行投影での評価は未実施 (T12c)。

## 完了したタスク

| ID | 個体 | 内容 | 結果 | コミット |
|---|---|---|---|---|
| T1 | deer | 角を太い三日月に。参照クラス図の左右の角の fy 行ごとの対応点から、左右対称を仮定して 3D 折れ線を復元 (主幹 13 点、前向き枝 2 本、内向き枝 1 本、根元は頭頂から外へ水平の帯) | glow IoU 0.21 → 0.39 | `25e4b9c` |
| T2 | wolf | 頭を長さ 0.5 m のブロック (平らな額、眉からの落ち、水平な顎)、首を前へ 0.32 m 伸びる太い楔に | 「獏に見える」解消 | `e7fa5ea` |
| T3 | wolf | 参照の比率 (鼻先→肩 0.77 H、肩→尻 0.55 H、尻→尾先 0.42 H) に合わせ頭+首 +0.28 m、尾 −0.18 m | 縦横比 1.73 | `e7fa5ea` |
| T4 | deer | `Kit.cut_along`: 参照座標の多角形の辺を通る平面で、辺の近くの面だけを bisect してから塗る (奥側は鏡像の平面) | dark IoU 0.31 → 0.52、境界が直線 | `25e4b9c` |
| T5 | wolf | 尾の上縁〜背〜首のシアン線を、輪郭から立てた薄い鰭 (`Kit.fin`, `screen=True`) に。参照の線は輪郭のすぐ内側 (幅 0.013 H) を走るので帯の内側の縁を折れ線にする | glow IoU 0.08 → 0.33 | `e7fa5ea` |
| T12 | 共通 | `compare_ref.py proj=ortho`、`tune_colors.py --proj ortho`、`Kit.setup_ref_camera(ortho=True)`。参照は 4 本の足を同じ地面線に描いているので、透視では奥の脚が高く写る (wolf 0.2 H) | wolf 0.58 → 0.63 (形状修正前) | `e7fa5ea` |
| T12b | deer | deer も平行投影を採用 | 0.49 → 0.52 (形状修正前) | `25e4b9c` |
| T15 | 共通 | `compare_ref.py` が `classrows.txt` (シルエットと各パーツの fy 0.02 刻みの行 / fx 0.05 刻みの列の画素区間、参照 \| モデル) を出力 | 形状合わせの反復が数値で追える | `7c67d82` |
| — | 共通 | `lowpoly_kit.tube` の先端で最後のリング頂点が孤立する不具合 (wolf 6 / deer 30 の LOOSE_VERTS) を修正 | `inspect_scene.py` PROBLEMS なし | `e7fa5ea` |

## 残タスク (優先度順)

優先度: P2 = PARTIAL の改善 (目標値到達)、P3 = 評価系・運用。規模: S = 1 反復、M = 数反復、L = 作り直しを含む。

### P2 形状・見え方

| ID | 個体 | タスク | 目標 | 規模 | 参照 |
|---|---|---|---|---|---|
| T5b | wolf | 手前の後脚の腿を大きく、脚を上腿・下腿の 2 節にして飛節で折る (旧 T9 と統合)。鼻梁を短く | シルエット 0.77 → 0.80 | M | wolf #1, #4 |
| T8 | deer | 耳を 0.28 に細く立てる (頭頂と耳の隙間)、胸の前面を y −0.17 へ、腿の上端 ry 0.09。目の縁を菱形に、口の線を追加 | シルエット 0.73 → 0.80 | S | deer #2, #4 |
| T1b | deer | 参照は奥の角を手前の 0.65 倍の太さで描いている。奥の角の半径を落とす (左右非対称) か参考値にするかを決める。主幹上部を +0.02 外へ。枝の付け根の深緑を根元だけに | glow 0.39 → 0.50 | S | deer #1, #3 |
| T6 | rabbit | 奥の耳の「折れ」を幾何で作る (斜めの折り線で 2 枚の平板に分ける)。ねじり方式は失敗済み | 他アングルでの見え方 | M | rabbit #1 |
| T7 | rabbit | 胸の断面を前面が平らな台形にし、六角模様が重ならないようにする (半径 0.85 倍) | glow 0.33 → 0.40 | S | rabbit #5 |
| T5c | wolf | 首の上の 2 本目の線と胸の前縁のリボンを鰭の折れ線に統合し、参照の途切れも再現する | glow 重心 0.06 → 0.03 | S | wolf #3 |
| T10 | rabbit | 耳の左右対称化の要否を決める (ゲーム内で対称が必要なら `make_ear` の引数を揃える) | 判断 | S | rabbit #2 |
| T11 | rabbit | 三角形数を目標中央 (2000〜2500) へ。胴の densify を 2 回に | 1863 → 2100 前後 | S | rabbit #8 |
| T11b | deer | 三角形数 2805 が上限 3000 に近い。パネル多角形の頂点を減らす (肩 11 → 8、腰 12 → 8) | −200 前後 | S | deer #6 |

### P3 評価系・運用

| ID | 対象 | タスク | 規模 | 参照 |
|---|---|---|---|---|
| T12c | rabbit | 平行投影での評価値を取り、透視 0.81 と比べて採用を決める (3 個体で評価条件を揃えるなら採用) | S | rabbit #4 |
| T13 | 共通 | dark クラスの「陰由来」成分を評価から除外するオプション。参照の濃色の大きな連結成分 (脚・腹・首の下の陰) を参照・モデル双方で fur に戻し、鼻・耳・蹄・パネルだけで比較する | S | rabbit #3, wolf #2 |
| T14 | 共通 | 収束した材質色をゲームのレンダラ (three.js) の照明で再検証し、必要なら同じ補正ループをゲーム側で回す。glow はエミッション強度で詰める | M | rabbit #7, deer #5, wolf #6 |
| T16 | 共通 | `rabbit.py` を `lowpoly_kit.py` に乗せ替える (現状は rabbit だけ独自実装。回帰確認が必要) | M | — |
| T17 | 共通 | ユーザー手動受入 (各 acceptance-criteria の D5 / D6): 比較画像の印象、glb を Blender / three.js で他アングル確認、ゲーム内照明 | — | D5 / D6 |
| T18 | 共通 | ブランチの push (LFS 転送の確認) と `feat/m8` または `main` へのマージ。下記「ブランチの状況」参照 | S | — |

## ブランチの状況 (T18 の材料、2026-09-20 11:00 時点)

- 分岐点 `b09d573` (feat/m4 の途中、2026-09-19 17:17)。このブランチの独自コミット 23、変更ファイル 70 (tools/blender、assets/models、assets/textures/concept、docs/design/qa、.gitattributes、.gitignore)。
- `main` は分岐点から 27 コミット (M4 末尾〜M8 計画、114 ファイル)、`feat/m8` は 47 コミット (M5〜M8-05、128 ファイル) 進んでいる。本体の checkout は `feat/m8`。
- 両側で変更したファイルは `.gitignore` の 1 つだけ。`main` / `feat/m8` を `--no-commit` で試しにマージすると、衝突は `.gitignore` 末尾のブロック
  (こちら: `# Python __pycache__/`、相手: `# 他エージェントの worktree .claude/worktrees/`) のみで、両方を残せば解決する。他は自動マージ。検証後 `merge --abort` 済み。
- リモートは未設定 (`git remote -v` が空)。push と LFS 転送の確認はリモート設定後。LFS 追跡は `assets/models/{rabbit,deer,wolf}.{blend,glb}` の 6 ファイル。
- 参照画像の旧バリエーション 12 枚 (`assets/textures/concept/*-v*.png`) は `archive/` へ移動してコミット済み (`484b632`)。作業ツリーは clean。

## 進め方の提案

1. P2 は個体ごとに 1 セッション: wolf (T5b + T5c)、deer (T8 + T1b + T11b)、rabbit (T7 + T6 + T11 + T12c)。各セッションの最後に色補正ループを回し直し、受入基準の B / C 表と残課題を更新する。
2. T13 は rabbit / wolf の dark 指標を「参考値」から「判定」に戻せるかを決めるためのもの。P2 の前か後に S サイズで入れる。
3. T18 (マージ) は P2 を待たずに実施できる。`.gitignore` の衝突だけ解決すればよい。
4. T14 はゲーム側のレンダラが固まってから。T17 は各段階の区切りで実施する。
